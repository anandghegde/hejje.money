# Hejje — Progress log

One entry per completed milestone. Newest at the bottom.

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| — | — | — | Plan created 2026-09-07 from hejje_prd.md Draft v1 |
| M0.1 | 2026-09-08 | (this commit) | Gradle 8.14.5 wrapper, Spring Boot 3.5.16, Modulith 1.4.13, Java 21 toolchain via foojay (local JDK 17 runs Gradle). `event_publication` registry table lives in `V1__baseline.sql` (Modulith schema init disabled). Health endpoint public until M0.3. Wrapper URL validation disabled (`validateDistributionUrl=false`) because the JVM could not HEAD services.gradle.org from this machine; Testcontainers on Colima needs `DOCKER_HOST=unix://$HOME/.colima/default/docker.sock` and `TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock`. |
| M0.2 | 2026-09-08 | (this commit) | `common`: Money/Price/Quantity/Ids (own UUID v7), enums, HejjeClock + JdbcHolidayCalendar (NSE 2026 weekday holidays seeded in V2, from Zerodha's calendar; re-verify against the NSE circular), events (HejjeEvent/EventMeta, TickBus contract), CorrelationIdFilter (UUID only; non-UUID headers are replaced), ApiExceptionHandler (RFC 7807), log redaction (pattern converter + JSON provider). `audit`: audit_event table with append-only trigger (V3), AuditService, `GET /api/v1/audit`. `MutableClock` lives in test sources. AuditEventType includes Phase 0 auth/egress types up front. Follow-up: correlation ids from TUI/agents must be UUIDs (documented in docs/events.md). |
| M0.3 | 2026-09-08 | (this commit) | JWT via Spring Security's Nimbus JOSE (HS256, zero clock skew), Argon2id via BouncyCastle, Bucket4j 8.19. Endpoints live under `/api/v1/auth/*` (plan wrote `/auth/*`; API base path rule applied). `ScopeCatalog` and `HejjePrincipal` live in `common.security` (not `auth`) to avoid an auth<->audit cycle. API-key secrets hashed with SHA-256 (high entropy, checked per request); passwords with Argon2id. Refresh tokens rotate on every refresh. Rate-limit buckets are in-memory per process. Follow-ups: session listing/forced logout not built; WS endpoints arrive in M1. |
| M0.4 | 2026-09-08 | (this commit) | `deploy/`: multi-stage Dockerfile (gradle:8.14-jdk21 -> temurin 21 JRE, non-root), prod compose (hejje + postgres + caddy auto-TLS), Caddyfile, `.env.example`, systemd unit, `backup.sh`, RUNBOOK. `system`: `EgressIpVerifier` (VERIFIED/MISMATCH/UNKNOWN/SKIPPED; disagreeing resolvers or empty expected-ips = MISMATCH; event + audit on change), `ClockDriftChecker` (HTTPS HEAD `Date`, >2 s = DEGRADED and blocking; unreachable = UNKNOWN, not blocking), `ExecutionReadiness` over `ReadinessCheck` beans, health in PRD §41 shape with `{status, detail}` per line. Prometheus on the management port 8081 in prod (Caddy never proxies it). Both checks are disabled (`SKIPPED`) in `dev`/`test` profiles. Deviation: `ExecutionProperties` (`hejje.execution.expected-ips`) lives in `common.config` until the execution module exists. Not done: the `docker compose up` on a real VM (no VM available in this session). |
| M1.1 | 2026-09-08 | (this commit) | `instruments` module (Instrument, HejjeSymbol, InstrumentService, sync job, endpoints) and the first slice of `broker` (`BrokerAdapter.brokerCode()/getInstruments()`, `BrokerInstrument`, `broker.zerodha.KiteInstrumentCsv` parser, minimal `broker.fake.FakeBrokerAdapter` serving a 33-row Kite CSV fixture). `InstrumentType`/`OptionType` enums live in `common`. Indices use exchange `INDEX` (Kite exchange/segment kept in the mapping). Natural key is `UNIQUE NULLS NOT DISTINCT`; upserts are batched (1000) with the mapping joined on the natural key. Sync deactivates by `synced_at` watermark. `weeklyExpiries` returns all upcoming option expiries (weekly + monthly). Integration tests now run on a `MutableClock` (`AbstractIntegrationTest.TestClockConfig`); tests that move it reset it. Follow-ups: ISIN not in Kite CSV (column stays null); a `broker` -> `instruments` dependency for token lookup must go through a port in `broker` (M1.2) to avoid a module cycle. |
| M1.2 | 2026-09-08 | (this commit) | `broker` module completed: full `BrokerAdapter` (session, quotes, history, streaming handle, orders, reads, funds, order margins), Hejje-side models, `BrokerException{kind, retryable, outcomeUnknown}`, `BrokerOrderUpdates` in-process bus, `BrokerSessionService` (+ `broker_session` V6, AES-GCM `TokenCipher` in `common.security`, readiness `brokerSession`, 06:15 IST expiry, 5-min validation 08:30–15:45 IST, restore on startup), endpoints under `/api/v1/broker/*`, Kite postback with checksum. `ZerodhaKiteAdapter` on `javakiteconnect` 4.0.1; `KiteClientFactory` sets the hard-coded base URL and OkHttp timeouts reflectively (documented deviation). Full `FakeBrokerAdapter` (fills, positions, funds, scripted failures, seeds, streams, async updates + `flush()`). Deviations: `Timeframe`, `Validity`, `MarketTick` live in `common` (broker must produce them; market depends on broker); broker→instruments lookup goes through the `BrokerInstrumentResolver` port implemented in `instruments.internal`; `BrokerCandle` is the broker-side candle (market's `Candle` arrives in M1.3); `PermissionException` maps to `AUTH`, Kite 5xx to `NETWORK`. Health line `broker` now reflects readiness `brokerSession`. Integration tests share a fixed `hejje.security.encryption-key` and a Kite test secret so contexts can restore each other's session rows. Follow-ups: Kite ticker reconnection is the library's own until M1.3 layers the streamer; order-margin call untested against WireMock (shape only); rate limiting and latency timers around adapter calls arrive in M1.6. |
| M1.4 | 2026-09-08 | (this commit) | `orders` module (OrderState = PRD §36 with an explicit transition table + `OrderStateMachine`, `IllegalTransition` audited; `OrderIntent`, `HejjeOrder`, `OrderEvent`, `Trade`, `Position`; stores; `OrderService` public facade; `PositionService` average-cost book with realized P&L; durable events) and `execution` module (`ExecutionEngine` pipeline: idempotency → persist intent → validation → risk → order READY → SUBMITTING → placeOrder → BROKER_ACCEPTED/REJECTED/UNKNOWN; `OrderValidator`; `IdempotencyStore` (24h); `RiskDecisionStore`; `BrokerUpdateListener`; `UnknownOrderResolver` (poll getOrders by tag ≤30s); `OrderPoller` (5s fallback); controllers; `/ws/events`; problem+json mapping). `risk` module bootstrapped with the public `RiskEngine` port + approve-all stub (real engine M1.5). V8 migration for all seven tables. Deviations: positions use average-cost (a single `average_price` column; Zerodha reports average too) rather than lot-level FIFO — realized P&L for the 3-fill test is average-cost; broker order tag is the last 16 hex chars of the order id (Kite tag ≤20 alnum); order transitions serialized per order id via an in-process lock so async broker updates racing ahead of the local ack skip illegal transitions instead of throwing; SUBMITTING/BROKER_ACCEPTED transitions broadened to accept early broker updates; `dropAck()` now suppresses the async update so the UNKNOWN→poll→OPEN path is exercised. Tests: transition-table (every legal edge + illegal rejected), market fill→position, 3-fill average+realized, partial→full, dropAck→UNKNOWN→resolve→OPEN, validation 422 (adapter never called), idempotent replay + concurrent-duplicate (one order), cancel. 190 tests green. Follow-ups: full reconciliation/rate-limiter/gate/bootstrap in M1.6; risk controls in M1.5; PaperBrokerAdapter in M1.7 (fake broker used meanwhile). |
| M1.5 | 2026-09-08 | (this commit) | `risk` module completed: `RiskEngineImpl` (replaces the M1.4 approve-all stub) building an `AccountSnapshot` via `AccountSnapshotBuilder` (realized/unrealized from positions + QuoteCache, trades today, consecutive losing round-trips reconstructed from trades, broker funds/margin, net & last-trade per instrument) and running `RiskControls` (17 pure checks); `risk_limits` (one row/mode, seeded defaults, `PUT /risk/limits`); kill switch (`kill_switch` table, `POST/GET/DELETE /risk/kill-switch`, auto-trip on daily-loss breach with reason DAILY_LOSS, destructive actions carried out by an execution-side `KillSwitchExecutor` via the `KillSwitchActivated` event); `PositionSizer` + `POST /risk/position-size`; `GET /risk` dashboard (PRD §54). Exposure-reducing intents skip limit checks except broker/readiness. V9 migration. Health `riskEngine` line now HEALTHY. Added `@JsonCreator`/`@JsonValue` to `Price` and `Quantity` (scalar JSON for the order/risk HTTP bodies and the web client). Deviations: risk↔execution cycle avoided by publishing `KillSwitchActivated` (risk) and consuming it in execution; RiskEngine port stays `evaluate(OrderIntent)` (engine builds snapshot/limits internally) so the M1.4 call site is unchanged; margin-utilization falls back to notional when the broker gives no margin. Tests: per-check table-driven (pass+fail each), PositionSizer (lot floor + cap), RiskIT (kill switch STOP_NEW_ORDERS rejects BUY, allows POSITION_CLOSE; daily-loss auto-trip; limits/dashboard/sizing/confirmation endpoints). Cross-test fixes: ExecutionIT relaxes risk_limits, RiskIT resets them, AuthIT resets the shared clock, WireMock log test iterates a copy. 206 tests green. |

| M1.6 | 2026-09-08 | (this commit) | ReconciliationService (30s/startup/on-demand; corrects local orders from broker source=RECONCILIATION, imports unknown broker orders source=EXTERNAL, position-qty mismatch = CRITICAL reconciliation_issue -> kill switch + reconciliation readiness red until resolved; endpoints under /api/v1/execution/*); ExecutorLease (single-row executor_lease, 10s heartbeat, readiness) + ExecutorBootstrap (PRD 63 order on ApplicationReadyEvent: lease -> reconcile -> restore in-flight orders -> enable execution, bootstrap readiness); BrokerRateLimiter (Bucket4j) inside a primary RateLimitedBrokerAdapter wrapper (orders fail fast RATE_LIMITED, reads wait; per-second/minute/day + quote/historical/general buckets; metrics); latency timers broker.call{op}/broker.ack/risk.evaluate + GET /server/latency (p50/p95/p99); LiveTradingGate composing broker session + readiness + kill switch, wired as the single check in OrderValidator. V10 migration. EXTERNAL order-event source; new audit types. Deviations: rate-limited wrapper is @Primary and injects the raw fake/zerodha adapter via ObjectProvider; kill switch surfaces on new orders through the gate (Validation) rather than the risk check; orderQueue health is a static 'no order queue (fail-fast)'; scheduling disabled under the test profile and the lease non-gating in tests (many contexts share one DB) - the lease SQL semantics are tested directly. Tests: rate limiter (11th order rejected before the adapter, quote bucket), wrapper (delegate not called past the limit), reconciliation (import unknown order; CRITICAL position mismatch trips kill switch + readiness, resolve clears), restore-in-flight (SUBMITTING -> OPEN via poll), executor lease contention. 213 tests green. Follow-ups: local --kill CLI + standby failover deferred; signal.to.ack/signal.to.fill timers are Phase 2. |

| M1.7 | 2026-09-08 | (this commit) | Cost model in `common.costs` (`CostModel`, `CostBreakdown`, `CostFill`, `Segment`, `CostProperties` from `config/costs.yaml` + classpath default, marked verify:true) shared by paper and (later) the backtester; per-fill fees applied at the orders layer (`Position.fees` + `netRealizedPnl()`, V11 `fees_paise`, `OrderService.cost()`); `broker.paper.PaperBrokerAdapter` (MARKET fills at last price +/- slippage, LIMIT/SL on crossing tick, simulated positions/funds, order updates via the shared bus, delegates quotes/history/instruments/streaming/session to the real adapter, never places a real order); mode wiring (`PaperBrokerAdapter` primary only when mode=PAPER and the real adapter is active; the fake is itself a paper sim in dev/test), `X-Hejje-Mode` response header, `GET /trades/{id}/costs`. Deviations: cost rates loaded via `spring.config.import` of costs.yaml into `hejje.costs.*` (record binding) rather than a bespoke YAML loader; realized P&L kept gross with a separate fees column and `netRealizedPnl()` helper; CONFIRM-with-open-PAPER-positions is a warning (not a refusal). Tests: cost model hand-computed (equity intraday buy+sell, delivery, futures, options), paper adapter (slippage MARKET, LIMIT crossing, SL both sides, broker.placeOrder never called), paper round-trip realized net of fees (98.87 fixture) + costs endpoint + mode header. Added `QuoteCache.clear()` for test isolation. 221 tests green. Follow-ups: WireMock zero-Kite-order assertion covered at the adapter level (Mockito verify) rather than a full zerodha+PAPER context. |

| M1.8 | 2026-09-08 | (this commit) | Web client v1 in `web/` (Vite + React 18 + TS, React Router, TanStack Query, lightweight-charts). Typed API client (bearer token in memory, refresh on 401 then redirect, client-generated Idempotency-Key on every transactional call), `/ws/events` and `/ws/market` hooks with backoff reconnect. Screens: Orders (+ manual order form with instrument resolve, side/type/qty, risk-based sizing preview, cancel), Positions (close/close-all, fees), Trades, Risk (dashboard §54, kill switch three buttons + typed CLOSE ALL confirm + re-arm), Broker (status/connect/logout), Server (health §41, latency, reconciliation issues resolve), Settings; Today/Pulse/Strategies/Lab/Hejje AI disabled placeholders; persistent mode banner (red LIVE / blue PAPER) + server/broker/market status dots. Vitest tests (API client bearer+idempotency, ApiError, risk sizing, paise format) pass; `npm run lint`, `npm test`, `npm run build` all green; no broker secret in the bundle (grep). Playwright smoke spec + config present (login -> broker -> place paper order -> Orders -> close). CI web job runs lint/test/build. Deviations: eslint flat-less classic config; Playwright smoke needs the full server+web stack, so CI runs lint/test/build and the smoke stack in CI is a follow-up. |

| M1.9 | 2026-09-08 | (this commit) | TUI in `tui/` (Go module `hejje.money/tui`, binary `hejje`). Typed `internal/api` client (API-key bearer, client-generated Idempotency-Key on transactional calls, problem+json APIError, WS `/ws/events` stream via gorilla/websocket); `internal/config` (server_url from `~/.config/hejje/config.yaml`, API key from `HEJJE_API_KEY` only). Cobra commands: status, server, broker, positions, orders, order, cancel, close, close-all, risk, logs, kill (`--cancel-all`/`--close-all`, typed CLOSE ALL / [y/N] confirmations), place (`--instrument --side --qty|--risk --type --price --stop --target`), all with `--json`. Bubble Tea dashboard (no-arg): mode banner, Best-Hejje 'No strategies deployed', positions w/ P&L, open orders, daily P&L vs limit, server/broker/market dots, keys c/x/K/r/q. `go vet ./...`, `go test ./...` (api client via httptest: bearer+idempotency, APIError, unique keys, ws URL; config: file + env, api key never from file), `go build ./cmd/hejje` all green (Go 1.27.1 installed via brew this session). `build.sh` cross-compiles linux/mac. CI tui job added. |

## Phase 0 exit

- All four milestones implemented; `cd server && ./gradlew clean build` green (61 tests) on 2026-09-08. CI workflow present but not yet run on GitHub (repository has no remote yet).
- VM hostname: **hejje.malgudi.app** (Ubuntu 24.04, 6 vCPU / 16 GB, Docker 29 + compose v5, provisioned 2026-09-08). Expected egress IP (`HEJJE_EXECUTION_EXPECTED_IPS`): **172.235.15.109** (also the DNS A record).
- Kite app redirect URL: `https://hejje.malgudi.app/api/v1/broker/callback`.
- Deployment deviates from the compose-only runbook: the VM already runs nginx on 80/443 for other sites, so nginx terminates TLS (Let's Encrypt) and proxies to `127.0.0.1:8090`; an untracked `deploy/docker-compose.host.yml` override on the VM disables the Caddy service (`profiles: ["caddy"]`) and publishes `hejje` on `127.0.0.1:8090`. Nightly `backup.sh` cron at 14:30 UTC on weekdays (see RUNBOOK §3a).
- Verified 2026-09-08 (from the VM logs and public endpoints): `/api/v1/server/ping` UP over TLS, egress IP `UNKNOWN -> VERIFIED (172.235.15.109)`, clock sync `HEALTHY` (drift 722 ms), `hejje` and `hejje-postgres` containers healthy.
- Local toolchain note: Gradle 8.14.5 runs on JDK 17 and provisions the Java 21 toolchain via foojay; the wrapper distribution download timed out from this machine, so builds here used a locally unpacked Gradle 8.14.5.

## Phase 1 exit

All milestones M1.1–M1.9 implemented and committed on `main` (2026-09-08). Server: `cd server && ./gradlew clean build`
green, 221 tests, `ApplicationModules.verify()` passes. Web: `cd web && npm ci && npm run lint && npm test && npm run build`
green. TUI: `cd tui && go vet ./... && go test ./... && go build ./cmd/hejje` green (Go 1.27.1 installed via brew this session).

Phase 1 exit checklist status:
- [x] PAPER: place, modify, cancel, fill, close; audit trail; fees visible (M1.4/M1.7 tests, `PaperTradeIT`).
- [ ] CONFIRM live 1-share round trip — needs a provisioned VM with a static IP registered at Zerodha and a real Kite login (not available in this environment).
- [x] Restart mid-session recovery and reconciliation without manual intervention (`ExecutionRecoveryIT`).
- [x] Duplicate request (same idempotency key, retried) creates exactly one broker order (`ExecutionIT`).
- [x] Broker logout during session: readiness red, new intents rejected with a reason, closes allowed (`BrokerSessionIT`, gate).
- [x] Egress IP mismatch disables execution and shows in health/TUI (`EgressMismatchIT`, `hejje status`).
- [x] Kill switch from Web, TUI, API and local paths (API + Web + TUI implemented; local `--kill` CLI is a documented follow-up).

Follow-ups carried forward: live CONFIRM round trip on a real VM; local `--kill` CLI and HA standby (Phase 5); wiring the
Playwright smoke stack into CI; verifying `config/costs.yaml` rates against the current Zerodha/NSE charge sheets.

| M2.1 | 2026-09-09 | (this commit) | `strategy` module: condition DSL in `strategy.dsl` (named interface; tokenizer + recursive-descent parser -> sealed `Expr` AST, `IndicatorCatalog` with arities/defaults, `ConditionEvaluator` over a `BarContext` port returning `EvalResult{status PASSED/FAILED/NOT_READY, observedLhs/Rhs}`), YAML `DefinitionParser` (Jackson YAML -> `StrategyDefinition` with `{path, message}` errors, unknown keys rejected, defaults applied) + `StrategyValidator` (semantic rules), V13 tables `strategy`/`strategy_version` (immutability trigger: only `status` may change, no deletes)/`strategy_deployment`, `StrategyLifecycle` transition table gated by the `StrategyEvidence` port (default bean = no evidence until M2.3), `StrategyService` (create/version/clone/status/deploy/pause, universe resolution incl. aliases), `BundledStrategyLoader` (filesystem dirs then `classpath:strategies/`; Gradle copies `../strategies` into the jar, Dockerfile copies `strategies/`), endpoints under `/api/v1/strategies` and `/api/v1/deployments`. Docs: `docs/strategy-dsl.md`, `docs/strategy-schema.json`. Deviations/assumptions: definition hash = SHA-256 of the canonical JSON of the parsed definition (not the YAML text) so comments/whitespace never create versions; `family` defaults to `trend` and bare universe names are aliases (`hejje.strategy.aliases`, NIFTY/BANKNIFTY/FINNIFTY -> nearest future) so the PRD 10 example validates verbatim; `trade_window` defaults to 09:15..force_exit_time; create/version bodies are JSON `{yaml, changeNote}` rather than raw YAML; PAPER deployments need a version in PAPER/LIVE and CONFIRM/AUTO need LIVE; pausing/retiring a version pauses its deployments; `ValidationError` uses `path` (the common validation problem uses `field`). JSON-schema validation is done by the hand-written parser (the schema file documents the same structure; no schema library added). Tests: 38 parser golden inputs + 27 error cases, evaluator (precedence, NOT_READY, crosses), definition parser/validator (PRD example, paths, semantic rules), lifecycle table, `StrategyIT` (API create/version/immutability trigger/DRAFT->LIVE 409/clone/deployments/bundled loader/scope). 317 tests green. Follow-ups: `docs/api.md` examples for `GET /strategies/{id}/versions/compare` come with M2.5; alias table may move to `config/aliases.yaml` in M2.4.
| M2.2 | 2026-09-09 | (this commit) | `market.indicators` (named interface): `IndicatorContext` per (instrument, timeframe) implementing the DSL `BarContext` (series histories, `SessionTracker`, registered indicators, `warmUp`, `registerDefinition`, lazy registration of unknown calls), `Indicator`/`AbstractIndicator` with per-bar `History` (offsets), and SMA, EMA, RSI, ATR, ADX, Bollinger, highest/lowest, session VWAP, opening range, relative volume, prev-day high/low/close, gap %, session minutes. Golden fixture `server/src/test/resources/indicators/golden.csv` (450 five-minute bars over six synthetic sessions) generated by `research/tools/gen_indicator_fixtures.py` with **TA-Lib** (pandas-ta's non-TA-Lib path uses a different RSI/ATR warm-up, so the C library was installed via brew and the venv is Python 3.12); every indicator matches within 1e-6 and is not ready exactly where the reference is NaN. Conventions documented in `docs/indicators.md` (TA-Lib seeding: SMA-seeded EMA, Wilder RSI/ATR/ADX with TA-Lib's n-1 seed for ADX, population std for Bollinger). Deviations/assumptions: previous-day levels are tracked from the intraday bars fed to the context (plus `seedPreviousDay` from a D1 candle) rather than read from a D1 store, so the backtester and live engine derive them identically; VWAP is not ready on zero-volume (index) series; relative volume uses the available prior sessions (up to n) rather than requiring n. Tests: golden (18 columns x 450 bars), offsets, opening range NOT_READY before 09:30 then constant/reset, VWAP per-session reset and index case, prev-day/gap/session minutes incl. seeding, relative volume slots, context guards, definition registration. |
| M2.3 | 2026-09-09 | (this commit) | `backtest` module: `BacktestEngine` (pure bar replay per instrument via `InstrumentReplay`: pending entry fills at next open, intrabar stop/target with both-touched = stop and gap-through fills at the open, close-of-bar trailing/VWAP target/rule exits/max holding/force exit, entry evaluation inside the trade window with evidence; `Levels` shared stop/target/trailing maths; `SessionSplitter` for NONE/FIXED/WALK_FORWARD; `MetricsCalculator` for every PRD 12.1 metric plus equity/drawdown curves and R/monthly/weekday/hour tables; `QualityChecker` for the PRD 12.3 warnings), `BacktestSpec/Input/Result/Trade/Metrics/QualityWarning/WalkForwardWindow` types, V14 `backtest`/`backtest_trade`, `BacktestStore`, `BacktestService` (submit/execute/runNow/prepare with warm-up candles from `MarketService`, risk-per-trade resolution), bounded `BacktestRunner` (workers, cancel flags, restart cleanup), endpoints under `/api/v1/backtests`, `BacktestEvidenceAdapter` (@Primary `StrategyEvidence`: BACKTESTED = any DONE run; VALIDATED = DONE run with an OOS slice >= 30 trades and no FAIL). Docs: `docs/backtesting.md`. Deviations/assumptions: `direction: both` is refused by the engine (one entry block cannot pick a side; follow-up: `entry_long`/`entry_short`); `max_trades_per_day` is per instrument; `atr_multiple` stops use `atr(14)` and swing stops `lowest/highest(10)`; R multiples are on net P&L; Sharpe/Sortino use daily net P&L over initial capital across every session (no-trade days = 0); walk-forward here evaluates sequential test windows without re-optimisation (parameters are never tuned by the engine); cost-model version is recorded but the current model is always used; queued/running runs are marked FAILED after a restart rather than resumed; `BacktestException` maps to 422 (404 for "not found"). Tests: engine (breakout day = exactly one trade with hand-computed entry/stop/target/qty/costs/evidence, both-touched -> stop, gap through stop, force exit at 15:10 + max trades/day, slippage rounding and BAR_CLOSE, determinism hash, no-trade + `both` refusal, end of data), metrics (hand-computed on 5 trades; empty/all-winning), quality (20 trades -> INSUFFICIENT_SAMPLE/CONCENTRATED/MISSING_DATA/OVERFIT_RISK; IS/OOS gap), `BacktestIT` (API submit -> runner -> DONE with metrics/trades/warnings, lifecycle BACKTESTED allowed and VALIDATED refused, delete cascades, runNow twice -> same hash, OOS split, bad specs). Also fixed a pre-existing test-isolation race: `MarketDataIT.candlesEndpointMergesRecentAndHistorical` depended on the 1-second scheduler closing a bar left open by the previous test; its setUp now flushes the candle builder. Test profile sets `hejje.strategy.load-bundled=false` so the bundled definitions (M2.4) never load into test databases. 340 tests green. |
| M2.4 | 2026-09-09 | (this commit) | Code and docs delivered; the data work itself is deferred to the VM. Six bundled strategies in `strategies/` (`nifty_orb`, `nifty_orb_breakdown`, `vwap_trend_continuation`, `vwap_reversion`, `pdh_pdl_breakout`, `ema_pullback`) with conventional, untuned parameters and a page each under `docs/strategies/` (rationale, rules in words, parameters, regime fit, weaknesses, baseline placeholder); `BundledStrategiesTest` parses and validates every file. `config/universe/nifty50.yaml` (constituent list as of 2026-09-09, flagged for verification). Market module: `ContinuousSeries` + `continuous_series` (V15) + `ContinuousFuturesBuilder` (pure `stitch`: contract for session d = earliest expiry strictly after d, i.e. roll on the expiry date, no back-adjustment; candles written under a stable series id) + `POST/GET /market/history/continuous`; `DataIntegrityReport` + `GET /market/history/integrity` (expected vs present sessions, missing list, per-session bar counts vs 375/125/75/25/7/1, synthetic count); `InstrumentService.futures(underlying)`. `StrategyService.universeTargets` expands aliases; `BacktestService` maps `nearest_future` targets to the continuous series when built (else today's nearest contract) and accepts `NFO:X:FUT:CONT` symbols. `docs/data.md` is the backfill runbook. **Not done here (needs a Kite session on the VM):** the 3-year backfill for indices/futures/NIFTY 50, the coverage report, and the baseline IS/validation/OOS + walk-forward backtests; strategies therefore stay `DRAFT` with baseline sections marked pending. Caveat recorded: Kite lists only live contracts, so expired-contract history cannot be fetched retroactively; the continuous series starts with the oldest contract captured. Statement per plan: no strategy parameter has been changed after seeing OOS results (none seen yet). Tests: stitching (roll day, no adjustment, post-expiry drop), `HistoryIT` (build via API, stitched candles, idempotent rebuild, integrity report with a short session and a holiday), bundled strategy validation. |
| M2.5 | 2026-09-09 | (this commit) | `scoring` module: `BaseScoreCalculator` (documented piecewise-linear maps for expectancy/profit factor/max DD(R)/consistency/sample size per split, OOS 60 / validation 25 / IS 15 renormalised over the splits present; walk-forward stability from the windows' expectancy std-dev; slippage sensitivity via `SlippageSensitivity` re-running the base backtest at 2x slippage through the new `BacktestService.evaluate` (cached per backtest id); cap at 50 with < 30 OOS trades), `ScoreAdjuster` port with `TechnicalCompatibilityAdjuster` (indicator context warmed from the last 5 days, entry conditions on the last closed bar, `round(-10 + 18 x passedShare)`) and `RecentPerformanceAdjuster` (last 20 round trips reconstructed from fills by strategy vs backtest expectancy in money, +/-5), `ScoringService` (compute per version x instrument, deployment instruments else universe; headline = best instrument), `strategy_score` (V16), `ScoreRefresh` (`BacktestFinished` listener + 5-minute session refresh of deployed versions), `ScoreController` (`GET/POST /strategies/{id}/score[/recompute]`, `GET /strategies/compare`, `GET /strategies/{id}/versions/compare` with `Comparisons` templated verdict). Backtest module gained `BacktestFinished` (published inside a `TransactionTemplate` so the Modulith listener fires), `evaluate(spec)` and `baseBacktest(versionId)`. Docs: `docs/hejje-score.md`. Deviations/assumptions: components are rounded to 0.1 so the base equals the sum within +/-0.35; neutral 50 for missing walk-forward windows or a failed sensitivity run; `strategyId` on trades/positions is the strategy (lineage) id, so recent performance is per strategy, not per version; similar-regime column is a Phase 3 placeholder; the headline score is the best instrument's. Tests: base formula (weights, split weighting arithmetic, cap, losing/none), adjuster bounds, verdict wording + round-trip pairing, `ScoringIT` (breakdown via API sums to final, adjusters' evidence, slippage evidence, compare and versions-compare endpoints; `BacktestFinished` triggers rescoring; unscored version). Also fixed a pre-existing flaky assertion in `AuthIT` (the secret was cut at the last underscore, but base64url secrets may contain underscores). 354 tests green. |
| M2.6 | 2026-09-09 | (this commit) | `signals` module: `StrategyRunner` per deployment x instrument (bar-for-bar mirror of the backtester's replay: previous-bar signal expiry, intrabar stop/target checks with software backup when no stop order is live, indicator update, trailing/VWAP/rule/max-holding/force-exit management, entry evaluation with evidence), `SignalEngine` (single engine thread, runner lifecycle from deployments + `DeploymentChanged`, warm-up from history, bus subscription for candles and ticks, order-event routing by order id or via intent -> signal, restart restore with stop verification/re-placement), `ExecutionPort` with `LiveExecutionPort` (SL-M stop with reason `STRATEGY_STOP` -> role STOP, market exits `STRATEGY_EXIT`, modify/cancel through the execution engine) and `SimulatedExecutionPort` (parity/replay), `SignalService` (list/active/prepare dry-run sized on the live quote/execute with Idempotency-Key/skip/expiry sweep), V17 `signal` + `strategy_position`, endpoints under `/api/v1/signals`, `signal.to.ack`/`signal.to.fill` timers, new audit types. `Levels` moved to the backtest public API; the replay's entry time for BAR_CLOSE is the signal bar close (max holding measured from it). Docs: `docs/signals.md`. Deviations/assumptions: targets are software-managed (market exit on tick/bar touch) since the broker has no OCO, documented; the trades-per-day counter counts executed signals; paused runners keep their state; execution/AUTO autonomy is Phase 5 (every execute is a human confirmation); positions are matched to orders through the intent's signal id when a fill beats the order-id write-back (the write-back patches only the id). Tests: `ParityTest` (all six bundled strategies x two seeded recorded sessions: identical signal times, entry prices, stops, targets, quantities, exit reasons between runner+simulated port and backtester BAR_CLOSE), `SignalsIT` (signal -> prepare (160 shares on the live quote, risk APPROVED) -> execute -> fill -> SL-M STOP order live -> stop fill closes with STOP and audit chain; max trades/day; expiry after the next bar and re-signal + skip; force exit at 15:10 with exit fill and stop cancel; restart re-attaches the runner and re-places a cancelled stop; pause/resume). 359 tests green. |
| M2.7 | 2026-09-09 | (this commit) | `recommend` module (`RecommendationService`: PRD 73 score x signal validity x risk eligibility per deployment x instrument -> TRADE/WAIT/AVOID with PRD 29 decision object and PRD 20 evidence/risks; `GET /today` with header/best/ranked/noTrade; `recommendation` history per signal decision change; `hejje.recommend.min-score`), `analytics` module (`AnalyticsService` round trips rebuilt from fills per instrument x strategy with version/signal links through `strategy_position`, `GET /analytics/pnl?groupBy=strategy|version|instrument|weekday|hour|regime`, `ReviewService` writing `trade_review` on `PositionChangedEvent` net 0 with outcome R, setup validity re-evaluated from stored candles, entry/exit slippage bps, rule adherence, UNKNOWN context placeholders; `/reviews` endpoints), V18. Fixed a Phase 1 gap: fills now carry the intent's strategy (`OrderService.recordFill` had a null placeholder), so attribution and per-strategy positions work. `SignalService.dryRun` (side-effect-free sizing + risk) and `positionForOrder`. Dev seeding: `POST /market/dev/candles`, `POST /broker/dev/quote`, forced status (`allow-forced-status`), `allow-off-session-paper`; fake broker order ids seeded per process so restarts against a persisted DB never collide. Web: Today (Best Hejje card, EXECUTE -> prepare modal with risk checks -> CONFIRM, DETAILS, SKIP, ranked table, No-Trade), Strategies list + strategy page (rules list, lifecycle, score breakdown, deployments, backtests with equity chart/trades/warnings, version compare + verdict), Lab (YAML editor with live validation, create/new version/clone, run backtest), Trades attribution + review link, Reviews, Analytics; nav updated; Vite dev proxy with relative API base (the server has no CORS; production serves the bundle behind the reverse proxy) and session restore from the refresh cookie on reload. Playwright `paper-flow.spec.ts` (seed -> signal -> Today -> execute -> fill -> stop -> review -> attribution) and the M1.8 smoke both pass against the dev stack (fake broker, Postgres container, jar run with Java 21); the flow ran after market hours thanks to the dev-only off-session paper switch and relaxed paper risk limits set by the spec. Deviations/assumptions: the paper flow executes through the API when the Today card is WAIT (no score >= 70 without a real backtest history) and through the card when it is TRADE; Best Hejje = top TRADE; review adherence is a simple 50/50 rule; the Today history is written on read; `store:false` seeding keeps shared history clean. Tests: `RecommendationIT` (no deployments -> WAIT -> TRADE with seeded score -> AVOID under the kill switch, history order, forced status audited), `AnalyticsIT` (manual round trip -> review with R, every groupBy sums to the trip, endpoints), `RoundTripsTest` (pairing, averaged prices, bps), web `today.test.ts`. Server 364 tests green; web lint/test/build green; `npm run e2e` 2 passed. |
| M2.8 | 2026-09-09 | (this commit) | TUI: typed client for `/today`, strategies/versions/score/deployments/backtests and signals (prepare without, execute with a fresh Idempotency-Key, skip); commands `best`, `strategies`, `strategy <id>`, `signals [--status]`, `execute <signal-id> [-y]` (prints the prepared order and risk checks, asks y/N), `skip <signal-id>`; dashboard Best Hejje card with `[E] Execute` (prepare -> y/N confirm) `[D] Details` `[S] Skip`. Renderers in `internal/ui/today.go` (PRD 8.2 card, PRD 20 details, prepared order, PRD 14 score table). Verified `hejje best` against the running dev stack. Also deduplicated sizing notes in Today's hard blocks. `go vet ./... && go test ./... && go build ./...` green. |

## Phase 2 exit

All milestones M2.1-M2.8 implemented and committed on `main` (2026-09-09). Server: `gradle clean build` green (364 tests, `ApplicationModules.verify()` passes; run with the locally unpacked Gradle 8.14.5 because the wrapper download still times out here). Web: `npm run lint && npm test && npm run build` green; `npm run e2e` (smoke + paper flow) 2 passed against a local dev stack (Postgres 16 container, the boot jar on the Gradle-provisioned Java 21, fake broker, PAPER). TUI: `go vet ./... && go test ./... && go build ./...` green.

Phase 2 exit checklist status:
- [ ] Six strategies validated (or explicitly flagged), docs with baseline metrics — the six definitions and their pages exist and validate; baseline backtests need the 3-year dataset backfilled on the VM with a Kite session (`docs/data.md`), so every version is still `DRAFT` and the baseline sections read "pending". Explicitly flagged.
- [x] Parity backtester <-> live for all strategies (`ParityTest`: all six on two recorded sessions, identical signal times, entry prices, stops, targets, quantities, exit reasons).
- [x] PAPER: signal -> Today -> confirm -> fill -> stop order placed -> close -> review; audit chain `SIGNAL_CREATED` -> `STRATEGY_RECOMMENDED` -> `SIGNAL_PREPARED` -> `USER_APPROVED` (intent + order ids) -> `ORDER_SUBMITTED`/`BROKER_ACCEPTED`/`ORDER_FILLED` -> `STRATEGY_STOP_PLACED` -> `POSITION_CLOSED` (`SignalsIT`, `paper-flow.spec.ts`).
- [x] Score breakdown documented and reproducible (`docs/hejje-score.md`; same backtest + market state -> same breakdown).
- [x] Trading core runs with `hejje.llm.enabled=false` (no LLM code exists yet; nothing in Phase 2 depends on one).

Follow-ups carried forward: VM backfill + baseline backtests + validation of the six strategies; `direction: both` needs `entry_long`/`entry_short` blocks in the DSL; live-mode (CONFIRM) round trip on the VM; Playwright e2e stack in CI; regime/news/event adjusters, `TRADE WITH CAUTION`, similar-regime comparison column and review context arrive with Phase 3; the recommend module writes history on read (a scheduled snapshot would be cleaner); the market-data readiness/quote cache ignore older-stamped ticks, which the dev seeding works around by stamping quotes "now".

## Phase 3

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| M3.1 | 2026-09-09 | (this commit) | `regime` module: PRD 13 enums (`Trend`, `Volatility`, `Opening`, `Breadth`, `IntradayStructure`, `EventEnvironment`, each with `UNKNOWN`), `RegimeSnapshot` (labels + `features` + one evidence sentence per dimension + classifier version + `finalLabel`), rules in `internal/RegimeRules` (docs/regime.md; thresholds in `config/regime.yaml` = `hejje.regime.*`, copied into the jar with `config/universe/*.yaml`), `DailyWalker` (EMA20/50, ADX14, ATR14, VIX and ATR/close percentile ranks over a trailing 250-session window, walked forward so no label sees later bars), `RegimeEngine` (live snapshot: index D1 + a partial D1 built from the day's M5 bars, VIX D1, index M5, constituent M5 for breadth; historical labelling: same walk over stored D1 with breadth from constituent D1 closes), `market_regime` (final row per session and version) + `market_regime_intraday` (V19), `RegimeService` (`current()` cached for the snapshot interval, `labelHistory` with a SHA-256 result hash, `labels(from,to)`), `RegimeRefresh` (5-minute intraday snapshots, final label at 15:35 IST, startup labelling of sessions missing under the current version = relabel on version bump), endpoints `GET /context/regime`, `/history`, `/intraday`, `POST /context/regime/label` (admin). `EventEnvironmentSource` port with a `NORMAL` default (M3.3 supplies the real bean). Backtest: `RegimeBreakdown` (`byRegime` along configurable dims, default trend × volatility, plus `similarRegime` vs the current snapshot) computed on read in `BacktestService.regimeBreakdown`, merged into `GET /backtests/{id}` and served by `GET /backtests/{id}/regimes?dims=`; the comparison column `similarRegimePerformance` is now filled. Scoring: `RegimeCompatibilityAdjuster` (−10..+10 = preferences part clipped to [−6, +3] + similar-regime expectancy part ±7 at ±0.5R with ≥ 10 trades, evidence per matched/unmatched key). Analytics `groupBy=regime` and the review `context.regime`/`breadth` now use the labels. Web strategy page shows the by-regime table and the similar-regime line. Deviations/assumptions: intraday inputs are M5 (the timeframe the runbook backfills); the index "VWAP" for structure is the equal-weighted session average because index candles carry no volume; breadth in historical labelling is advance/decline only (no VWAP share); `regime_preferences` keys map to label groups (`trending`, `ranging`, `volatile`, `quiet`, `gap`, `expiry`, or any exact lower-case label); reversal is checked before trend so a full-range reversal is not a trend day; the test profile points the breadth universe at a five-symbol test file because a per-test property would boot a second application context against the shared database (which broke `SignalsIT`). Follow-ups: nothing in the UI shows the regime yet beyond the backtest block (Today header/Pulse arrive with M3.2/M3.5); a stale-history guard (labels only refresh when D1 data lands) is not built. Tests: `RegimeRulesTest` (every dimension at its thresholds, preference keys), `RegimeGroupingTest`, `RegimeIT` (live snapshot over 130 synthetic sessions, progressive intraday snapshot + cache, reproducible label hash, UNKNOWN without data, adjuster evidence, `GET /backtests/{id}` with `byRegime`). |
| M3.2 | 2026-09-09 | (this commit) | `pulse` module: `TechnicalPulse` (direction × strength, −100..100 score as the weight-normalised sum of ten rules over the rules with inputs, `coverage`, one `PulseComponent` per rule with value/contribution/evidence), `MarketPulse` (PRD 16.2 rows: regime/volatility/breadth labels from the regime snapshot, one `SectorStrength` row per `config/universe/sectors.yaml` entry, global context `NEUTRAL` placeholder), rules in `internal/PulseRules` (docs/pulse.md; weights and thresholds in `config/pulse.yaml`), `PulseInputs` over M5 candles (index vs session average, day change, ROC, VIX change, sector relative strength, nearest future basis and relative volume vs the same time of day over 10 sessions), `market_pulse` (V20) written every minute during the session by `PulseRefresh`, `PulseService.current()` cached per interval, `GET /context/pulse` and `/context/pulse/history?date=`. Sector indices added to the default watchlist and `MarketProperties`. Today header now shows `regime` (trend), `regimeLabels` and `breadth` (`UNKNOWN` without data). Web `/pulse` (composite, Market Pulse table, sector bars, VIX sparkline from `/market/candles`, evidence) + nav; TUI `hejje pulse` (`RenderPulse`). Deviations/assumptions: relative volume uses the nearest NIFTY future (index candles carry no volume) and only ever confirms the day's direction (never flips it); the index "VWAP" is the equal-weighted session average; the fake fixture only has NIFTY BANK and NIFTY FIN SERVICE, so the other sector rows are `UNKNOWN` in dev; `global context` has no source yet. Tests: `PulseRulesTest` (bullish/bearish/neutral fixtures, missing inputs drop out of the weighting with per-row UNKNOWN sectors, empty inputs), `PulseIT` (seeded index/VIX/sector/future sessions on a 2024 date: BULLISH composite, component values, sector labels incl. a missing-index UNKNOWN row, endpoints, stored history, Today header), TUI `TestRenderPulse`, web `pulse.test.ts`. Test-suite hardening found necessary here (class order in the Gradle worker is not fixed and shifted after the new tests): `AuditIT` no longer uses `@AutoConfigureMockMvc` (it booted a second application context, i.e. a second signal engine and lease heartbeat against the shared database), `AbstractIntegrationTest` drains outstanding Modulith publications before every test and exposes `awaitAsyncListeners()` (used after `deploy()` in `SignalsIT`/`RecommendationIT`, because the async `DeploymentChanged` listener restarts the runner and dropped bars published before it), `ExecutionIT` resolves stale reconciliation issues in setup like its siblings, and the regime/pulse ITs seed data on 2024/2025 sessions no other test (or the 420-day regime lookback) touches. Full suite green three runs in a row (386 tests). |
| M3.3 | 2026-09-09 | (this commit) | `events` module: `MarketEvent` + `EventType` (PRD 18.1/18.2) + `market_event` (V21, upsert by source + deterministic key), `EventSource` port with `ComputedEventSource` (holidays from the calendar, weekly/monthly expiries from option expiries per underlying, configured rebalance dates), `CuratedYamlSource` (`config/events/macro-2026.yaml`: Budget, RBI MPC FY26/27, FOMC 2026 dated the next IST morning, India CPI, US CPI/jobs with confidence < 0.9 where unconfirmed; maintenance procedure in docs/events.md), CSV import (`POST /events/import`, lenient parser reporting per-line errors), manual `POST /events`, optional `NseCorporateActionsFetcher` (flag off; fails silently), `EventRiskEvaluator` (results today HIGH, macro within 60 min / in progress HIGH else MEDIUM, expiry day for the underlying's derivatives MEDIUM, ex-date/board meeting MEDIUM, next event + minutes), `EventRules` (`event_rules` → triggered/block/caution with a templated reason), `EventService` (risk never throws: LOW + unavailable), refresh on startup/07:00/on demand, endpoints `GET /events`, `GET /events/risk`, `POST /events`, `POST /events/import`, `POST /events/refresh`. Risk: new `RiskCheckContributor` port in the risk module (evaluated after the built-in controls for exposure-adding intents only, failures never block) implemented by `EventRuleCheck` (`eventRule`, rejects STRATEGY_SIGNAL intents whose version blocks). Recommend: `Decision.TRADE_WITH_CAUTION`, `nextEvent` on recommendations, `eventRisk` filled, block → hard block/AVOID, caution → TRADE_WITH_CAUTION, header `eventRisk`/`nextEvent`, `regime` = trend × volatility. Scoring: `EventRiskAdjuster` (−8/−3/0). Regime: `CalendarEventEnvironment` (@Primary) derives the event environment. Audit: `EVENT_ADDED`, `EVENTS_IMPORTED`, `EVENTS_REFRESHED`. `InstrumentService.weeklyExpiries(underlying, asOf)` added for calendars that look back. Web: Today calendar widget (next 7 days), event risk/next event on the header and Best card, caution colour; TUI best card shows event risk and next event. Deviations/assumptions: `hejje.events.enabled` defaults to true because the computed/curated sources are internal data (the only external source, NSE, is off by default per README rule 7); FOMC entries are dated the following IST morning (00:30) so the Indian session that reacts carries the FED environment; `EventRuleCheck` resolves `SignalService` lazily (bean cycle through the execution port); the strategy-page "Next Event" line uses the first deployment instrument. Follow-ups: 2026 US CPI/jobs dates carry confidence 0.6–0.7 until checked against bls.gov; `index-rebalance-dates` is empty until NSE announces; `TRADE_WITH_CAUTION` from VIX/RR/news/regime cautions arrives in M3.5. Tests: `EventRiskEvaluatorTest` (thresholds at 59/60/61 min, in progress, results/ex-date/board meeting/expiry/derivative mapping, rules with windows), `EventSourcesTest` (CSV rows + errors, curated YAML range filter, NSE classification/date parsing), `EventsIT` (refresh idempotent, holiday and expiry events, HIGH→MEDIUM→LOW around the RBI decision, environments RBI/FED/EXPIRY/NORMAL, manual add + CSV import + per-instrument risk, audit rows, adjuster −8/0), `EventRulesIT` (block → AVOID with eventRule hard block and a 4xx on execute; caution → TRADE_WITH_CAUTION as Best Hejje with a passing eventRule check). Test infrastructure: `@Scheduled` jobs were running in the test context after all (a library enables scheduling, so `SystemConfig`'s `@Profile("!test")` guard never applied; the 30-second signal expiry sweep and the one-minute score/regime/pulse refreshes explained the remaining order-dependent failures), so `AbstractIntegrationTest` now installs a no-op `taskScheduler`; full suite green twice in a row (396 tests). |
| M3.4 | 2026-09-09 | (this commit) | `llm` module (minimal, completed in M4.1): `LlmProvider` (complete + default single-chunk `stream` over `Flow.Publisher`), `OpenAiCompatibleProvider` (`/chat/completions`, key from the env var named by `api-key-env`, JSON mode, timeouts, 429/5xx/timeouts retryable), `FixtureLlmProvider` (`type: fixture`: canned answers by prompt hash / substring / fallback, records requests), `LlmProperties` with PRD 66C profiles (`fast, reasoning, news, research` → provider + model/temperature/max-tokens), `LlmService` (routing, retries with doubling backoff, one `llm_call` row per attempt: profile, provider, model, purpose, prompt version, prompt **hash**, tokens, cost estimate from `pricing`, latency, correlation id, status, error), `Prompts` (versioned templates under `resources/prompts`, `{{placeholder}}` fill, hash), `StructuredOutput` + `JsonSchemaLite` (type/required/properties/enum/min/max/items; one retry with the validation errors). `news` module: `news_source` seeded from `config/news-sources.yaml` (4 Indian market RSS feeds, reliability 0.7–0.75), poller every 5 min (secure XML parsing for RSS/Atom, JSON arrays, HTML stripped), dedupe by URL + content hash + normalised-title Jaccard ≥ 0.8 within 24 h, `InstrumentMatcher` over `config/aliases.yaml` (all 50 NIFTY constituents with company names/tickers/sector, generated from the universe file), `NewsClassifier` (profile `news`, `prompts/news_classify_v1.txt`, schema-validated JSON, invented symbols dropped, no candidates → no call, `max-items-per-poll` cap), `news_assessment`, `NewsBiasAggregator` (direction × materiality × confidence × novelty × reliability × 0.5^(age/4 h), Σ × confirmation 1 + 0.25 × (sources − 1) ≤ 1.5, clipped, labels at ±0.2/±0.6, evidence per story + confirmation + price/volume reaction from M5 candles), `news_bias` written on read, `NewsService.bias` never throws (unavailable when news off, LLM off, or no successful poll within 2 h), endpoints `GET /news`, `GET /context/news-bias`, `GET/PUT /news/sources`, `POST /news/poll`. V22. Scoring: `NewsContextAdjuster` (round(3 × score), −3..+3, 0 with reason). Recommendations carry `newsBias`; web `NewsBiasPanel` (label, score, expandable evidence + source links) on the Best card and the strategy page; Best card also shows the regime key. Defaults: `hejje.llm.enabled` and `hejje.news.enabled` false (`HEJJE_LLM_ENABLED`, `HEJJE_NEWS_ENABLED`); the test profile enables both with the fixture provider only. Deviations/assumptions: novelty multiplies the weight (the plan's formula lists it as a dimension; 0 = already discounted); the price/volume reaction is an evidence line, not a factor; `news_item.body` is stored but never fetched (feeds only); Gemini/Anthropic adapters and real streaming are M4.1; no TUI surface for news (not in the plan). Tests: `LlmUnitTest` (disabled → Unavailable with no log row, profile defaults + call log without prompt text + cost, retries then success / non-retryable, structured output retry and double failure, schema subset), `NewsUnitTest` (dedupe, RSS/Atom/JSON parsing incl. refused external entities, aggregation arithmetic/decay/confirmation/labels/clipping), `NewsIT` (ingest with URL/story duplicates, fixture classification incl. an invented symbol, bias + evidence + reaction line, endpoints, call-log hygiene, adjuster, source update; stale feed and unclassified items degrade to NEUTRAL). |
| M3.5 | 2026-09-09 | (this commit) | `context` module: `ContextItem` (GREEN/AMBER/RED/UNKNOWN, value, delta, evidence) and `StrategyContext` (technical fit, market regime, news bias, event risk, sector, next event, net impact) built by `ContextService` from the live adjusters (`ScoringService.adjustments`, new, non-persisting), the event risk, the news bias and the Market Pulse sector row of the instrument's alias sector (`NewsService.sectorOf`, new); `GET /context/strategy?versionId=&instrumentId=`. Recommend: `DecisionRules` (pure PRD 15 table, docs/decisions.md), `Caution{code,message}` with `EVENT_CAUTION`, `VIX_RISING` (`PulseService.vixChangePct`, new), `REWARD_RISK` (below the strategy's `min_reward_risk`, above the global), `REGIME_AVOID`, `NEWS_OPPOSING` (±0.4 against the direction), `MARKET_DATA_STALE` (quote missing or cache-stale); the trading-window control now yields WAIT instead of AVOID; recommendations carry `cautions[]` and `context`; supporting evidence/risks gain the card's GREEN/RED rows as templated lines ("✓ Strategy performs strongly in the current regime", "⚠ News bias Bearish -0.6"); Best Hejje = first TRADE or TRADE_WITH_CAUTION. `hejje.recommend.min-score` is env-overridable (`HEJJE_RECOMMEND_MIN_SCORE`) for the e2e stack. Web: Best card shows the decision, cautions, news panel and the Context Card (expandable rows, next event, net impact, Pulse link); strategy page shows the Context Card; ranked table colours TRADE WITH CAUTION. TUI best card prints decision, cautions and the CONTEXT block. Playwright paper flow now deploys with `event_rules: caution`, injects a results event for today and asserts `TRADE WITH CAUTION` with the event in the cautions and a rendered Context Card (stack started with `HEJJE_RECOMMEND_MIN_SCORE=0`). Deviations/assumptions: the stale-quote caution uses the quote cache's 5 s flag because anything past the 10 s readiness threshold is already a hard block; the VIX-rising check reads the pulse's VIX component; the sector row needs an alias sector name that matches a `sectors.yaml` row (IT, Banking, Financials, Auto, Pharma, FMCG, Metal; others show UNKNOWN); the degradation test is a unit test with every service mocked as down (a runtime toggle of the beans is not possible in the shared context) plus the e2e run with the context flags off. Tests: `DecisionRulesTest` (every row of the table), `ContextServiceTest` (all services down → five UNKNOWN rows, net 0; thirds mapping), `ContextIT` (REWARD_RISK + NEWS_OPPOSING via the fixture LLM + MARKET_DATA_STALE → TRADE_WITH_CAUTION as Best Hejje with the card's rows and `GET /context/strategy`; fresh quote clears the stale caution), TUI `TestRenderBestAndNoTrade` extended; `npm run e2e` 2 passed against the dev stack (fresh Postgres + jar with `HEJJE_RECOMMEND_MIN_SCORE=0`) both with the context services on (card reads TRADE WITH CAUTION with the injected results event) and with `HEJJE_REGIME_ENABLED=false HEJJE_PULSE_ENABLED=false HEJJE_EVENTS_ENABLED=false E2E_CONTEXT_OFF=1` (plain TRADE, Context Card rows Unknown, trade flow unchanged). Fixed on the way: strategy event rules measured their window against the *next* calendar event instead of the event that made the risk HIGH (`EventRisk` now carries `trigger`/`triggerMinutesTo`); the DSL requires `high_risk_event_within_minutes` unless the action is allow (docs corrected). Note for reruns: restart the e2e stack on a fresh database, because a killed server leaves a live executor lease and unresolved reconciliation issues that block readiness. |

## Phase 3 exit

All milestones M3.1-M3.5 implemented and committed on `main` (2026-09-09). Server: full suite green (locally unpacked Gradle 8.14.5, Testcontainers Postgres); web: lint/test/build green; TUI: vet/test/build green.

Phase 3 exit checklist status:
- [x] Today shows regime/breadth/event/news adjustments with evidence (header: regime, breadth, event risk, next event; Best card: adjustment rows, cautions, news panel, Context Card with expandable evidence).
- [x] Every session in the historical store is regime-labelled (startup job over the index daily history, `POST /context/regime/label`); strategy detail shows similar-regime performance (backtest regime breakdown + comparison column). The VM backfill is still pending (Phase 2 follow-up), so locally the store holds only test sessions.
- [x] With LLM, news and events disabled, Phase 2 exit tests still pass: the defaults are off for LLM and news; the server suite runs the Phase 2 ITs unchanged, the context beans degrade to UNKNOWN rows, and the e2e paper flow passes with `HEJJE_REGIME_ENABLED=false HEJJE_PULSE_ENABLED=false HEJJE_EVENTS_ENABLED=false` (see the e2e note below).
- [x] `docs/regime.md`, `docs/decisions.md`, `docs/hejje-score.md` (regime, event, news adjusters), plus `docs/pulse.md`, `docs/events.md`, `docs/llm.md`, `docs/news.md`.

Follow-ups carried forward: VM backfill + regime labelling of the real history + baseline backtests; verify the curated 2026 US CPI/jobs dates and add the NSE index rebalance dates; the NSE corporate-action fetcher is untested against the live site; Gemini/Anthropic adapters, native streaming and the agent tool surface are M4.1+; `global context` in the Market Pulse has no data source; the `sectors.yaml`/`aliases.yaml` sector names must stay aligned by hand.

## Phase 4

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| M4.1 | 2026-09-10 | (this commit) | `llm` module completed. Provider-neutral conversations and tools: `LlmMessage` (user / assistant with tool calls / tool result), `LlmTool` (name, description, JSON schema), `LlmToolCall`; `LlmRequest` gains `messages` + `tools` (old constructor kept, `chat(...)` factory, `promptText()` hashed for the log), `LlmResponse` gains `toolCalls` + `finishReason`, `LlmChunk` carries the full response on the last chunk, `LlmStreams.cold` (a publisher that starts producing only once subscribed; the M3.4 default could drop chunks). `OpenAiCompatibleProvider` rewritten: messages/`tools`/`tool_calls`/`role: tool`, arguments parsed from the JSON string, native SSE streaming with `stream_options.include_usage` and tool-call fragments assembled by index. New native `GeminiProvider` (`generateContent` / `streamGenerateContent?alt=sse`, `x-goog-api-key`, `systemInstruction`, `functionDeclarations` with schemas reduced to Gemini's OpenAPI subset, `functionCall`/`functionResponse` parts, `responseMimeType` for JSON mode, prompt-block handling). `LlmService`: retries with ±50 % jitter, per-provider `ProviderCircuits` (5 consecutive retryable failures → open 60 s → one half-open trial; non-retryable failures do not count), optional `profiles.<name>.fallback` (once, never after streaming started), `stream(request, onDelta)`, JSON-mode temperature 0 default, `hejje.llm.daily-cost-cap` (rupees per IST day from the call log's estimates → `BudgetExceeded` + one `LLM_BUDGET_EXCEEDED` audit alert per day), `status()`. Endpoints `GET /agents/llm/status` (`market:read`) and `POST /agents/llm/test` (`admin`). `FixtureLlmProvider` gains a scripted `responder` (tool calls) and word-by-word streaming. Deviations/assumptions: the "recorded" HTTP fixtures are hand-written from the providers' documented response shapes (no API keys in this environment); the alert is the audit event plus a WARN log (notifications are Phase 5); cost is only as good as `pricing` (unpriced calls count as zero toward the cap); the Anthropic adapter stays a later option (PRD 66C lists it as potential). Tests: `OpenAiCompatibleProviderContractTest` (prompt/params/usage, tool-call round trip, streamed text, streamed tool call, 429/400/503 mapping), `GeminiProviderContractTest` (system instruction + JSON mode, function-call round trip with sanitised schema, stream, blocked prompt, 503), `LlmResilienceTest` (circuit open → half-open → closed, non-retryable does not trip, fallback profile with both attempts logged, cost cap + single alert + IST day start, JSON temperature, streaming via the service, scripted tool calls), `LlmStatusIT` (status, admin-only test call, unknown profile), existing `LlmUnitTest`. Full suite 435 tests green. |
| M4.2 | 2026-09-10 | (this commit) | New `agent` module. `AgentTool{name, description, requiredScope, transactional, inputSchema, inputType, outputType, handler}` (typed `of`/`transactional` factories), `AgentToolProvider` beans → `ToolRegistry` (sorted, duplicate names fail startup), output schemas generated from the output records by `ToolSchemas` (primitives required; outputs serialized without nulls), `AgentToolService.invoke`: scope from the credential only → input schema (`JsonSchemaLite`, now with `additionalProperties: false`, `format` uuid/date, min/max length and items, type lists) → typed handler → output schema → `agent_action` row + `AGENT_TOOL_CALLED` audit event (actor AGENT, client source) for every call including refusals; statuses OK/INVALID_INPUT/FORBIDDEN/NOT_FOUND/UNKNOWN_TOOL/CONFLICT/FAILED/INVALID_OUTPUT/UNAVAILABLE; large outputs summarised with their ids. V23 `agent_session` (credential, principal, profile, purpose) + `agent_action`. 18 read tools (`get_market_snapshot, get_market_regime, get_pulse, get_news_context, get_event_calendar, list_strategies, get_strategy, get_strategy_rankings` (PRD 29 decision objects), `get_strategy_backtest, compare_strategies, compare_strategy_versions, get_strategy_signal, get_positions, get_orders, get_trades, get_account_risk, calculate_position_size, get_audit_trail`) returning compact DTOs with citable ids and Hejje symbols. Presets on `POST /auth/clients` (`preset: research|execution`, `AgentPresets` in `common.security`; none includes orders:execute/cancel, positions:close, risk:write, admin). Endpoints `GET /agents/tools`, `POST /agents/tools/{name}` (caller's credential, `X-Agent-Session`, problem+json with `toolStatus`), `GET /agents/sessions[/{id}]`; optional task done: `POST /mcp` minimal MCP server (streamable HTTP, JSON responses: initialize, ping, tools/list filtered by scope, tools/call through the same service). Docs: `docs/agents.md`, generated `docs/agent-tools.md` (`AgentToolsIT` fails when stale; regenerate with `HEJJE_REGEN_DOCS=1`), `docs/api.md`. Deviations/assumptions: tool scopes mirror the REST reads (positions/orders/trades `market:read`, account risk and sizing `risk:read`, audit trail `admin`), so the `execution` preset cannot read account risk (its `prepare_order` dry run carries the risk decision, M4.4); `get_strategy` renders rules as the DSL's canonical condition text; `get_strategy_rankings` calls `RecommendationService.today()`, which records history for signal-bearing rows as the Today screen does; direct calls share one `direct` session per credential per IST day. Tests: `AgentToolServiceTest` (OK path recorded + audited, FORBIDDEN from the credential with the refusal recorded, malformed input incl. smuggled fields never reaches the handler, handler errors → statuses, invalid output caught, catalog filtered by scope, registry/tool checks, output summarisation), `AgentPresetsTest`, `JsonSchemaLiteTest`, `AgentToolsIT` (catalog with scopes/schemas, presets over HTTP, research key reads + FORBIDDEN on `risk:read`/`admin` tools with session trace and audit rows, session header ownership, malformed input, every read tool against real services with a strategy created from the bundled ORB, MCP initialize/list/call/refusal/401, docs in sync). The ITs create keys through `ClientCredentialService` because `/auth/clients` shares the admin's transactional rate bucket with `AuthIT`. Full suite 451 tests green. |
| M4.3 | 2026-09-10 | (this commit) | Hejje AI. `HejjeAiService`: system prompt `prompts/hejje_ai_v1.txt` (role, PRD 66E non-responsibilities, tool rules, citation rule; version logged per call), one `chat` agent session per conversation, tool loop (`LlmService.stream` with the caller's permitted tools as `LlmTool`s; every call through `AgentToolService`, so scope/schema/audit are the same as the REST path; at most `hejje.agent.ai.max-steps` = 8 LLM steps; tool results fed back as JSON truncated at 12k chars, refusals as `{"status":"FORBIDDEN",…}`), profile `reasoning` for a conversation's first question and `fast` for follow-ups (last 10 turns replayed), `GroundingChecker` (numbers in the answer must be in this turn's tool outputs, allowing rounding and fraction→percentage; cited ids must appear there; dates/times/ids/bare integers < 10 are not claims) with unverified claims returned, never dropped. Canned analyst flows (`AnalystFlows`: `why_ranked_first` = rankings + score breakdown + context rows + rules, `compare` = two strategies or two versions, `working_today` = P&L by strategy + current decisions) detected from the question or chosen explicitly; their tool outputs become a templated evidence block. New tool `get_pnl_breakdown` (`market:read`, over `AnalyticsService.pnl`); ranking decision objects now carry the Context Card rows. V24 `agent_conversation`/`agent_message` (grounding + trace per answer). Endpoints `GET /agents/ai/status`, `POST /agents/ai/ask` (JSON, or SSE `tool`/`delta`/`done`/`error` written on the request thread so the security context and correlation id cover the tool calls), `GET /agents/ai/conversations[/{id}]`; dev/test-only `POST /agents/llm/dev/fixture`. Web `/agent` (nav "Hejje AI"): streamed answer, quick prompts for the flows, tool-call trace panel (tool, scope, status, latency), unverified numbers highlighted, disabled state when the LLM is off. TUI `hejje ai "question"` (`--flow`, `--json`) and interactive `hejje ai` (`/new`, `/quit`); the Go client now sends `Accept: application/json`. Deviations/assumptions: "FAST for follow-ups" is read as follow-up questions of a conversation; follow-ups replay prior questions and final answers but not tool outputs (cited ids let the model re-fetch), so numbers repeated from an earlier turn without a new tool call are flagged unverified (conservative); the fixture LLM stands in for a real model in every test. Test-suite fix found here: `HejjeAiIT` is the first IT (alphabetically) that deploys a strategy, which subscribes an instrument and switches market streaming on for the rest of the shared context; `BrokerSessionIT.statusLoginUrlLogoutAndLoginRoundTrip` used the real clock and so failed during market hours whenever streaming was on (it would equally fail after SignalsIT under a different class order). It now pins the clock to a closed session like `ServerHealthIT`; unsubscribing in `HejjeAiIT` was tried and made `AnalyticsIT`/`PaperTradeIT` stale instead, so it was reverted. Tests: `HejjeAiIT` (five canonical questions call the expected tools: working today → pnl + rankings, why ranked first → rankings + strategy with the evidence in the prompt, compare → list + compare, regime → regime, positions and risk → positions + risk; fabricated 97 flagged while 500 / 1,000 from sizing verify; 8-step limit; follow-up on `fast` with the replayed conversation and stored messages; research key → `get_account_risk` never offered and refused when called; SSE stream and SSE error; status), `GroundingCheckerTest`, `HejjeAiDisabledTest`, web `agent.test.ts` (SSE parsing across chunks, error event, highlighting), TUI `TestRenderAiTurnMarksUnverifiedNumbersAndListsTools`, `TestAiInteractiveContinuesTheConversation`. Full suite 463 tests green; web lint/test (18)/build green; TUI vet/test/build green; `npx playwright test` 3 passed (smoke, paper flow, new `agent-chat.spec.ts`) against the dev stack with the fixture LLM (`HEJJE_LLM_ENABLED=true --hejje.llm.providers.fixture.type=fixture --hejje.llm.profiles.{reasoning,fast}.provider=fixture`). |
| M4.4 | 2026-09-10 | (this commit) | Agent-prepared orders with human confirmation. Policy engine `risk.policy` (named interface): `PolicyEngine.decide(PolicyRequest{action ORDER_NEW/MODIFY/CANCEL/POSITION_CLOSE, actorType, mode, autonomyLevel, eventRisk, score, newStrategyVersion})` → `ALLOW / REQUIRE_APPROVAL / DENY` with the deciding rule, reason and per-rule trace; pure `evaluate` (first enabled matching rule by priority, default REQUIRE_APPROVAL, non-user actors capped at REQUIRE_APPROVAL); `policy_rule` seeded from PRD 49 (daily loss → DENY new exposure, autonomy > 3 → DENY, agent at autonomy < 2 → DENY, event risk HIGH / version not LIVE / agent / manual / score < 80 / strategy → REQUIRE_APPROVAL); `GET /risk/policies` (`risk:read`), `PUT /risk/policies/{id}` (`risk:write`, ALLOW refused, `POLICY_UPDATED`). Agent side: `OrderProposals` (signal proposals via `SignalService.dryRun`; manual `{instrument, side, riskRupees, stop, entry?, target?, product?, strategy?}` sized by `PositionSizer`, dry-run `RiskEngine.evaluate`, policy with the deployment's autonomy level, event risk, score, LIVE-ness; re-validation at approval), `ApprovalService` (PROPOSED `order_intent` + PENDING `approval` + `AGENT_RECOMMENDED`; request `Idempotency-Key` dedupes proposals; approve = pending/unexpired/same mode, an agent credential never approves its own proposal, fresh policy + risk re-check (failure → FAILED + `APPROVAL_FAILED`, 422), atomic claim, `USER_APPROVED`, then the normal pipeline — signal proposals through `SignalService.execute` so the strategy stop is placed, manual ones as a USER intent with reason `AGENT_PROPOSAL`, modify/cancel/close through `ExecutionEngine`; replay by key; reject → `USER_REJECTED`; expiry sweep + lazy expiry → `APPROVAL_EXPIRED`; intents end SUBMITTED or DECLINED), `/ws/events` `approval` pushes via a new `common.ClientNotification`. Tools (`orders:prepare`): `prepare_order` (dry run; policy DENY → new tool status `DENIED`), `submit_order_intent`, `modify_order_intent`, `cancel_order_intent`, `close_position_intent`. Endpoints `GET /approvals[?status]`, `GET /approvals/{id}`, `POST /approvals/{id}/approve|reject` (`orders:execute`, `Idempotency-Key`). V25. Hejje AI prompt `hejje_ai_v2` (proposals only; never claim an order was placed). Web: Approvals inbox (proposal rows, policy, risk dry run, countdown, approve / reject with reason, recently decided), header badge + nav count refreshed by `/ws/events`, Hejje AI "Proposal created — approve in the inbox" banner. TUI: `hejje approvals [--status]`, `hejje approve <id> [-y]`, `hejje reject <id> [--reason]`, dashboard "⚑ N approval(s) waiting" line. Deviations/assumptions: "human credentials only" = `orders:execute` (no agent preset has it) plus the requester-can't-approve rule for API keys (the local user may approve what Hejje AI proposed for them); approvals of signal proposals expire with the signal, others after `hejje.agent.approvals.ttl` (5 min); agent proposals without a strategy use `account-autonomy-level` (3), a named strategy without an enabled deployment counts as 0; the PROPOSED intent is the audit anchor and the executed order gets its own intent (linked in the approval result); the policy is consulted for agent proposals and approvals only (manual orders and signal executions keep their own confirmation). Follow-up: after a modify, the fake broker holds the new limit but the local `hejje_order.limit_price` keeps the old one (pre-existing in the modify path, also for the manual endpoint). Tests: `PolicyEngineTest` (PRD table: autonomy 0/1/4 DENY, 2/3 approval, event HIGH and non-LIVE ahead of the agent rule, daily loss blocks new exposure but not closes and is fetched lazily, strategy score rows, manual, disabled rules, ALLOW capped), `PolicyIT` (seeded rules in order, read vs write scope, ALLOW refused, audit, 404), `ApprovalsIT` (execution key prepare → submit → PROPOSED intent → agent cannot approve → Idempotency-Key required → approval fills in PAPER → replay / other key 409 → audit AGENT_RECOMMENDED → USER_APPROVED → RISK_CHECK_PASSED → ORDER_SUBMITTED; cancel/modify/close approvals; expired approval → 409 + EXPIRED + DECLINED; daily-loss breach in between → 422 FAILED; autonomy-1 deployment → DENIED for prepare and submit, autonomy-3 → REQUIRE_APPROVAL; research key FORBIDDEN; self-approval 403; rejection final; proposal idempotency), web `approvals.test.ts`, TUI `TestRenderApprovalShowsPolicyRiskAndResult`, `TestApproveAndRejectAreIdempotentPosts`. Full suite 476 green; web lint/test (21)/build green; TUI green; `npx playwright test` 4 passed (new `approvals.spec.ts`: execution key submits, badge, inbox approve). Exit check on the dev stack: an `execution` key submitted a BUY on NSE:SBIN, `hejje approve <id> -y` (TUI, human key with `orders:execute`) approved it, the order filled in PAPER, audit `AGENT_RECOMMENDED → USER_APPROVED → ORDER_INTENT_CREATED → RISK_CHECK_PASSED → ORDER_SUBMITTED → ORDER_FILLED`; a research key got FORBIDDEN. `PolicyIT` edits with its own `risk:write` key: `/api/v1/risk` is a transactional rate path whose admin bucket other ITs share. |
| M4.5 | 2026-09-10 | (this commit) | Performance investigation. Trade reviews now record the context as of the entry: `event` (`EventService.riskAt(instrument, entry)`, new, with `eventTrigger`) and `news` (latest stored news-bias snapshot within the aggregation window before the entry via `NewsService.biasSnapshotAt`/`NewsStore.latestBias`, new, with `newsScore`); UNKNOWN when a source has nothing. `GET /analytics/pnl?groupBy=` gains `family`, `eventContext`, `newsBias`, `exitReason`. `AnalyticsService.facts` (round trip + review context = `TradeFact`) and pure `PerformanceMath`: loss attribution (losses = |net| of losing trades; buckets by family/strategy/trend/regime/event/news/exit reason/hour/instrument and family × trend with loss share; templated headline from the largest family × trend bucket), slippage (entry/exit bps mean/median/p90/worst, cost ≈ bps × price × qty, per strategy), rule adherence (mean %, fully adherent, invalid setups, manual exits, net of adherent vs partly adherent), counterfactual (removes the trades matching every given category, AND across categories and OR within one; recomputes trades/net/peak-to-trough drawdown/win rate/profit factor; `basis: SIMULATED`, hypothetical note, actual figures alongside). Reports `LossReport`/`SlippageReport`/`AdherenceReport`/`CounterfactualReport` with the period; endpoints `GET /analytics/losses|slippage|adherence`, `POST /analytics/counterfactual` (`market:read`, month to date by default). Agent tools `get_loss_attribution`, `get_slippage_stats`, `get_rule_adherence`, `run_counterfactual`; Hejje AI `losses` flow ("what lost me money this month") composes them and runs a counterfactual on the largest family × trend loss bucket, with ACTUAL and SIMULATED evidence lines kept apart. Web Analytics: new dimensions plus a "Loss investigation" section (period, attribution tables, slippage, adherence, counterfactual panel with Actual and SIMULATED columns). Deviations/assumptions: the counterfactual removes trades from the actual sequence and does not model replacement trades, different fills or re-entries (stated in every result); event/news context is captured only for reviews written from now on (earlier reviews read UNKNOWN); news context depends on a bias snapshot having been computed around the entry (Today and recommendations compute them for deployed instruments); the TUI needs no change because the flow runs server-side (`hejje ai "What lost me money this month?"`). Tests: `PerformanceMathTest` (hand-computed five-trade fixture: shares 83.3/16.7, headline, slippage costs 30.00 + 15.50, adherence 62.5 %, counterfactual net −50 → 450 and drawdown 600 → 100, AND/OR filter semantics), `PerformanceIT` (two paper round trips on a results day with a bearish snapshot → review context, new P&L dimensions, attribution, counterfactual with SIMULATED basis and 400 on an empty filter, tools default to month to date), `HejjeAiIT.whatLostMeMoneyRunsTheLossInvestigationFlow`, web `performance.test.ts`. Full suite 482 green; web lint/test (24)/build green. |
| M4.6 | 2026-09-10 | (this commit) | Natural-language strategy builder. `StrategyBuilder` (agent): the LLM (profile `reasoning`, temperature 0, prompt `strategy_builder_v1` embedding `docs/strategy-dsl.md` — copied into `prompts/` by `processResources` so the prompt cannot drift from the doc — plus the bundled `nifty_orb` and `vwap_reversion` as examples) writes YAML; `StrategyService.validate` checks schema + semantics; errors (`path: message`) are fed back as a conversation for up to 3 fixes; a valid result is saved as a DRAFT with change note `NL draft` — a new strategy (taken names get `_nl`, `_nl2`, …) or, with `strategy`, that strategy's next version (name forced to its slug; an identical definition is reported, not saved). `StrategyDraft` returns the YAML, the rules in words, every attempt with its errors and the parent YAML. `RuleWords` (strategy module, pure): a definition in plain sentences (conditions from the DSL AST: "the close is above the 15-minute opening-range high", stop/target/trailing/window/sizing/regime preferences/event rules). Tool `create_strategy_draft` (`strategies:write`, transactional; FAILED after three fixes) and `POST /strategies/drafts` (201 / 422 with the attempts / 503 without the LLM). Web Lab "Describe a strategy" panel: draft, rules in words, YAML or a line diff against the parent version, Run backtest / Edit (opens the version in the editor) / Discard (retires the version). Deviations/assumptions: "the DSL doc" is the whole `docs/strategy-dsl.md`; Discard retires the version because versions are immutable and never deleted; the builder does not change statuses and no agent tool does, so the M2.1 lifecycle keeps drafts away from LIVE. Tests: `StrategyBuilderIT` (PRD 22 sentence → valid DRAFT with the expected canonical conditions, stop, 2R target and rules in words, prompt/profile/temperature checked, second run gets `_nl`; an invalid answer fixed on the second attempt with the errors fed back; giving up after 4 attempts saves nothing, REST 422 and tool FAILED; an NL draft cannot move to LIVE, PAPER, VALIDATED or BACKTESTED without evidence and no tool changes status; a draft for an existing strategy is version 2 with the parent YAML and the forced name; `research` key FORBIDDEN), `RuleWordsTest`, web `diff.test.ts`. Full suite 490 green; web lint/test (26)/build green. |
| M4.7 | 2026-09-10 | (this commit) | Strategy experiment agent. `backtest.experiments` (named interface): `experiment` + `experiment_variant` (V26); variants are deltas on the base YAML (`VariantDelta`: JSON merge patch + `entry_add`/`exit_add`, renaming refused) validated like any definition (invalid ones kept as INVALID with errors, not run); the baseline is always variant 0; the dataset is the base version's judged backtest (instruments, range, split, slippage) unless the request overrides it; variants run through the same engine on unsaved definitions (`BacktestService.evaluate(spec, definition)`, new) on a bounded pool (`hejje.backtest.experiments.parallelism`, default 2); leftovers after a restart are marked FAILED. `ExperimentRanking` (pure, deterministic): per criterion 1 point for the best down to 0 for the worst (ties share), weighted mean × 100 over criteria every variant has — out-of-sample expectancy 0.35 (falls back to validation, then overall), OOS profit factor 0.15, max drawdown R 0.15, trade count capped at 100 0.10, simplicity (parameters + conditions) 0.15, walk-forward stability (std-dev of window expectancy) 0.10; warnings `IS_OOS_GAP`, `LOW_TRADES` (< 100), `PARAMETER_AT_EDGE` (value at the end of a swept range of three or more), FAIL-level backtest warnings; notes `MULTIPLE_COMPARISONS` (≥ 3 variants) and `NO_OUT_OF_SAMPLE`; verdicts BASELINE / NOT_BETTER / BETTER_BUT_FRAGILE / RECOMMENDED / BETTER_OUT_OF_SAMPLE. Promotion creates the next version as a DRAFT ("promoted from experiment … variant …"); nothing changes a status. Endpoints `POST /experiments`, `POST /experiments/preview`, `GET /experiments[?versionId]`, `GET /experiments/{id}`, `POST /experiments/{id}/variants/{variantId}/promote`; audit `EXPERIMENT_STARTED`/`EXPERIMENT_FINISHED`. Agent: `ExperimentAgent` (profile `research`, prompt `experiment_agent_v1` preferring OOS improvement, robustness, simplicity, adequate trades and lower drawdown over in-sample return, with the DSL reference and the base backtest summary; structured JSON output; every delta validated) and tools `propose_variants` (`strategies:read`), `run_experiment` (`strategies:write`), `get_experiment`. Web Lab: experiments of the latest version with ranks, verdicts, metrics, warnings and "Promote to version"; TUI `hejje experiments [id] [--version]` (read-only). `StrategyService.readTree/writeTree` made public for deltas. Deviations/assumptions: variant backtests are not stored as `backtest` rows (a backtest belongs to a saved version), so `experiment_variant` keeps the metrics summary and the result hash instead of a `backtest_id`; the Python worker (M2.9) does not exist, so every variant runs on the Java engine; walk-forward stability only counts when the split is walk-forward. Tests: `VariantDeltaTest`, `ExperimentRankingTest` (hand-computed two-variant scores 61.1/38.9; robust beats fragile with IS≫OOS, low-trade and edge warnings; order-independent), `ExperimentIT` (seeded TCS week: preview, run with an invalid delta, ranks 1–3 with LOW_TRADES and notes, baseline verdict, promote → v2 DRAFT, base untouched, second promote 409), `ExperimentAgentIT` (fixture `propose_variants` → two valid deltas and one invalid with errors, prompt/profile checked; `run_experiment` → `get_experiment` DONE), web `experiments.test.ts`, TUI `TestRenderExperimentShowsRanksVerdictsAndWarnings`. Full suite 497 green; web lint/test (28)/build green; TUI green. |

## Phase 4 exit

All milestones M4.1–M4.7 implemented and committed on `main` (2026-09-10). Server: full suite green (498 tests, `ApplicationModules.verify()` passes; locally unpacked Gradle 8.14.5, Testcontainers Postgres). Web: lint, test (28), build green. TUI: vet, test, build green. `npx playwright test` against the dev stack (fresh Postgres, the boot jar, fake broker, PAPER): 4 passed with the fixture LLM (smoke, paper flow, Hejje AI chat, approvals inbox) and 4 passed with `HEJJE_LLM_ENABLED=false` (the chat spec then checks the disabled state; the approval flow needs no LLM).

Phase 4 exit checklist status:
- [x] An `execution` agent key prepares an order, a human approves in the TUI, the order fills in PAPER; audit shows `AGENT_RECOMMENDED → USER_APPROVED → RISK_CHECK_PASSED → ORDER_SUBMITTED` — run on the dev stack with the built `hejje` binary (`hejje approvals`, `hejje approve <id> -y`; NSE:SBIN BUY filled; audit `AGENT_RECOMMENDED → USER_APPROVED → ORDER_INTENT_CREATED → RISK_CHECK_PASSED → ORDER_SUBMITTED → ORDER_FILLED`), and in `ApprovalsIT`.
- [x] A `research` key cannot call any transactional tool — `AgentToolsIT.aResearchKeyCannotCallAnyTransactionalTool` sweeps every tool the catalog marks transactional (`submit/modify/cancel_order_intent`, `close_position_intent`, `create_strategy_draft`, `run_experiment`) plus `prepare_order`: all FORBIDDEN; the refusals are recorded and audited.
- [x] NL strategy drafts cannot reach LIVE without backtest, validation, and human status changes — `StrategyBuilderIT` (DRAFT only; LIVE, PAPER, VALIDATED and BACKTESTED refused without evidence; no tool changes a status).
- [x] `hejje.llm.enabled=false` leaves the Phase 1–3 tests passing, with one qualification: the suite run with the LLM off (`SPRING_APPLICATION_JSON='{"hejje":{"llm":{"enabled":false}}}'`) passed 480 of 498; the 18 failures are the tests of LLM features themselves (Hejje AI 8, NL builder 5, experiment proposals 1, the LLM test endpoint 1) and the three Phase 3 tests that exercise LLM news classification by design (`NewsIT` 2, `ContextIT`'s news-opposing caution 1). Every other Phase 1–3 test, and the e2e above, passes with the LLM off; the default configuration keeps it off.
- [x] LLM daily cost cap enforced and visible — `hejje.llm.daily-cost-cap` blocks calls for the rest of the IST day with one `LLM_BUDGET_EXCEEDED` audit alert (`LlmResilienceTest`); `GET /agents/llm/status` shows today's usage, the cap and `budgetExceeded`.

Follow-ups carried forward: from Phases 2–3 — VM backfill, baseline backtests and regime labelling of real history, curated event dates, the Playwright stack in CI. From Phase 4 — a smoke test against a real LLM provider on the VM (`POST /agents/llm/test`; the provider fixtures here are hand-written from the documented response shapes because no API keys are available in this environment); the local `hejje_order.limit_price` is not refreshed after a modify (pre-existing, also for the manual endpoint); experiment variants keep a metrics summary rather than `backtest` rows; news context on reviews depends on a bias snapshot existing near the entry; the MCP endpoint is minimal (JSON responses only, no server-initiated stream or MCP sessions); an Anthropic adapter remains optional (PRD 66C); notifications for approvals and the budget alert arrive with Phase 5.

## Phase 5

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| M5.1 | 2026-09-10 | (this commit) | Live-vs-backtest drift. `analytics.drift` (named interface `drift`): per deployment, the newest `trailing-trades` (30) reviewed strategy trades closed within the last `trailing-sessions` (60) trading sessions (outcomes = the reviews' R, a win = positive net; a trade belongs to a deployment through its strategy position, so PAPER and CONFIRM/AUTO deployments are separate) against the base backtest's out-of-sample slice (whole run when it has no OOS trades, stated in the evidence). `DriftMath` (pure): one-sided binomial p-value of the wins under the backtest win rate, percentile bootstrap interval of the expectancy (2000 resamples, 90 %, fixed seed), expectancy ratio, drawdown multiple (R), profit factor on R; levels FAILED → DEGRADING → WATCH from `config/drift.yaml` (any met criterion; defaults WATCH p < 0.20 / expectancy < 50 % / DD ≥ 1×, DEGRADING p < 0.05 / upper bound < backtest expectancy / DD ≥ 1.5×, FAILED upper bound < 0R / DD ≥ 2×) with templated `triggered` sentences. `DriftService.evaluate`: stores `drift_state` (V27, plus `drift_assessment` history and `strategy_deployment.size_multiplier`), audits `STRATEGY_DRIFT_CHANGED` on a status change, and runs the status's configured actions (`ALERT` = `/ws/events` `drift` + WARN log, `LOWER_SCORE` = rescore with the new `DriftAdjuster` −15..0 at order 50, `REDUCE_SIZE` = size multiplier 0.50 applied to the risk per trade in `SignalEngine.riskPerTrade` and pushed into running runners, `MOVE_TO_PAPER` = `StrategyService.moveToPaper`, `PAUSE` = deployment paused + `STRATEGY_PAUSED` with the statistics, actor SYSTEM) only when the status is worse than the worst status already acted on and the deployment is enabled (pause fires once; an improvement lowers the mark). Triggers: after every post-trade review (its deployment) and `DriftSweep` every `hejje.drift.interval`. Manual override `POST /deployments/{id}/drift/override {reason}` (`strategies:write`): suppresses actions for the current status and anything no worse, restores the size multiplier, audited `STRATEGY_DRIFT_OVERRIDDEN`; a worse status or HEALTHY ends it. `GET /strategies/{id}/drift` (`strategies:read`): fresh report + state + last five assessments per deployment. Web: strategy detail "Live vs backtest drift" panel (PRD 25 table, criteria, evidence/history, Override…) and size on deployments; TUI `hejje strategy <id>` DRIFT section. Deviations/assumptions: a fifth status `INSUFFICIENT_DATA` (fewer than `min-trades` = 10, or no completed backtest) so a handful of trades never reads HEALTHY or acts; "trailing-N (30 trades and 60 sessions)" read as the newest 30 within 60 sessions; ALERT is audit + WebSocket + log until the M5.5 channels exist; MOVE_TO_PAPER moves the deployment, not the version status (the lifecycle has no LIVE → PAPER edge) and is not enabled by default; size reductions are never undone automatically (the override restores 1.00); re-enabling a paused deployment by hand without an override does not re-pause it until the status improves and relapses. Tests: `DriftMathTest` (binomial against exact rational sums, deterministic bootstrap, drawdown/PF on R, synthetic streams crossing each level: insufficient, healthy, watch on expectancy ratio, degrading on the win-rate test, failed on a negative interval and on the drawdown multiple, config defaults and point clamping), `DriftIT` (DEGRADING → size 0.50 once with ALERT / LOWER_SCORE −10 / REDUCE_SIZE, sizing halves, one assessment and one audit row over repeated evaluations; FAILED → one pause with the statistics, no second pause after a manual re-enable, adjuster −15; override over REST (400 blank, 200, size restored, adjuster 0 with the reason, no repeat actions) then a FAILED relapse pauses and ends it; insufficient data + REST view + 409 override + 404; MOVE_TO_PAPER on a CONFIRM deployment redeploys in PAPER and is skipped on a PAPER one), `ScoringIT` now expects the drift adjuster, `docs/agent-tools.md` regenerated (audit type enum), web `drift.test.ts`, TUI `TestRenderDriftShowsStatusSidesAndCriteria`. Full suite 513 green; web lint/test (30)/build green; TUI vet/test/build green. |
| M5.2 | 2026-09-10 | (this commit) | AUTO mode and autonomy 4-5. `hejje.mode=AUTO` needs the prod profile **and** `hejje.auto.acknowledged=true` (`ModeProfileGuard`); banner `● LIVE · AUTO` (web, TUI). New `auto` module: `AutoExecutor` decides each new signal of an autonomy 4-5 deployment (a fallback-executing transactional listener on the signal event handing off to one worker thread, since the engine publishes outside a transaction, plus a sweep on startup and every `hejje.auto.sweep`): skipped unless enabled/in mode/actionable; kill switch STOP_NEW_ORDERS → refused before any order; otherwise `PolicyEngine.decide` with actor STRATEGY and the deterministic context (qualification, the deployment's daily budget from the new `SignalService.deploymentDay`, event risk, latest score); autonomy 4 turns an ALLOW for a second entry on the same instrument and day into REQUIRE_APPROVAL (level 5 re-enters within the budget). ALLOW → `SignalService.executeAuto` (same sizing, validation, risk, kill switch, gate; actor STRATEGY, client `AUTO_CLIENT`, idempotency key `auto:<signal>`, audit `AUTO_EXECUTED` with decision, rule, trace and context; `execute` and `executeAuto` share `submitSignal`); REQUIRE_APPROVAL → audit `AUTO_HELD` + `AutoExecutionHeld` event → the agent module's `ApprovalService.proposeHeldSignal` (ORDER_NEW approval requested by the strategy, `requestedByType: STRATEGY`, no agent session (V28 drops that NOT NULL), AUTO policy kept, one per signal, expiring with the signal, audit `APPROVAL_CREATED`); DENY → signal BLOCKED with the rule. Policy (V28): `autonomy_above_phase` removed; `deployment_budget` (15, new DEPLOYMENT_BUDGET_EXCEEDED → DENY) and `auto_strategy` (85, new AUTO_ELIGIBLE: STRATEGY, autonomy ≥ max(4, minLevel), qualified, scored → ALLOW); `PolicyRequest` gains `autoQualified` + `budgetBreach` (old constructor kept); ALLOW is capped for everything but the user and qualified strategy signals at 4-5 (agents never); `PUT /risk/policies` accepts ALLOW only on AUTO_ELIGIBLE rules. Deployments: autonomy 0-5; 4-5 refused on CONFIRM; on AUTO only after `hejje.auto.min-paper-trades` (30) closed paper trades of the version (`strategy.PaperTradeEvidence`, implemented by signals). Level 5 self-pauses on drift ≥ `hejje.auto.self-pause-drift` (DEGRADING) in `DriftService`. Web: `/risk/policies` (inspect/edit priority, decision, params, enabled; ALLOW offered only on AUTO_ELIGIBLE) linked from Risk; approvals label strategy-held requests. Deviations/assumptions: autonomy 4-5 is also allowed on PAPER deployments as a rehearsal with simulated fills (live AUTO needs the prod profile, so this is also how the path is tested, and how a version accumulates the paper trades live AUTO requires); for paper, "promoted for the mode" = PAPER or LIVE and no paper-trade minimum; an unscored signal never auto-executes and score < 80 still needs a human (the PRD 49 rows are read as all applying, with auto last); stops, trailing, targets and exits are deterministic runner actions at every level already (M2.6), so level 5's extra powers are re-entries and self-pause; the daily loss budget uses gross realized P&L of the deployment's strategy positions (fees excluded); a kill-switch refusal leaves the signal ACTIVE (humans are blocked by the pipeline anyway). Tests: `PolicyEngineTest` (seed mirrors V28; qualified 4/5 ALLOW; unqualified/unscored/level 3 → approval; score, event HIGH, new version, budget, daily loss ahead of auto at level 5; agents never ALLOW even with an allow-everything rule; lowered minLevel cannot automate level 3), `PolicyIT` (new table order; ALLOW accepted on `auto_strategy` only), `ModeProfileGuardTest` (AUTO in prod needs the acknowledgement), `AutoExecutionIT` (eligible signal fills as actor STRATEGY with `AUTO_EXECUTED` and no human approval; event HIGH → one approval requested by the strategy, re-offer adds none, approving it fills through the confirmation path; kill switch → DENIED, no intent; level-4 re-entry → approval; level-5 re-entry within budget executes; spent budget → BLOCKED; chaos: engine stopped mid-trade, stop lost at the broker, restart re-places the stop and restores the runner, sweep/redelivered event/re-offer place no second entry; LLM provider throwing → AUTO unaffected and no `llm_call`; deploy rules for CONFIRM, level 6, AUTO with 0/29/30 paper trades), `DriftIT.autonomyFivePausesItselfOnceDriftIsDegrading`, web `policies.test.ts`. Full suite 525 green (the one failure in the full run was `docs/agent-tools.md`, stale because of the new audit types; regenerated, `AgentToolsIT` rerun green); web lint/test (33)/build green; TUI vet/test green. |
| M5.3 | 2026-09-10 | (this commit) | Smart, basket and split orders (execution module). **Smart intents** (PRD 33): `POST /orders/intents` accepts `targetPosition` instead of `side` + `quantity` (400 when both); `ExecutionPlanner` (pure `plan(current, target)`: −50 → +100 = BUY 150) works against the net position of (server mode, instrument, product, strategy); zero delta → `200 {noop: true, plan}`, otherwise `201 {plan, order}`. **Baskets** (PRD 34): `basket` + `basket_leg` (V29), `BasketService.submit` (idempotent by client + key): the broker's `getOrderMargins` for all legs together vs available funds first (a basket that does not fit is FAILED before any leg; margin unavailable → noted and legs risk-checked one by one), then a worker places legs through the normal pipeline one at a time (key `basket:<id>:<n>`), hedge legs first, each waited on (`OrderWaiter`, business-clock deadline plus `leg-timeout`) until filled before the next; ALL_OR_NOTHING stops at the first failed/unfilled leg (a leg working at the deadline is cancelled), remaining legs SKIPPED, and `CLOSE_FILLED_LEGS` closes filled legs with market orders in reverse placement order (new reason `BASKET_ROLLBACK`, exposure-reducing, role EXIT); BEST_EFFORT places every leg (PARTIAL). Statuses PENDING/EXECUTING/COMPLETED/PARTIAL/FAILED/ROLLED_BACK/EXPIRED; each leg keeps its order, state, placement order and detail (refusals name the failed risk checks via the new `ExecutionException.describe`); audit `BASKET_CREATED`/`BASKET_FINISHED`; `POST/GET /baskets`, `GET /baskets/{id}`. **Splits** (PRD 35): `split_order` (V29), `SplitPolicy{maxChildQuantity, delayMs, priceTolerancePct, cancelOnMove, deadlineSeconds}` on an intent (or automatically above `hejje.execution.planning.auto-split-above`, off by default); the whole intent is validated and risk-checked once, then children (whole lots) go through the pipeline one at a time, `delayMs` apart, each waited on, linked by `hejje_order.parent_order_id` (`OrderService.setParent/childrenOf`, new); an adverse move beyond the tolerance from the reference (limit, else last price) cancels (`cancelOnMove`) or pauses until the deadline, which cancels a working child (EXPIRED); `GET /orders/splits[/{id}]`, `POST /orders/splits/{id}/cancel`; audit `SPLIT_STARTED`/`SPLIT_FINISHED`. Interrupted baskets/splits are marked FAILED on startup. Agent tool `submit_basket_intent` (`orders:prepare`, transactional) → approval kind `BASKET_NEW` (policy as any agent proposal; approving submits the basket with reason `AGENT_PROPOSAL`). Web: Orders page "Baskets" (legs in placement order) and "Split orders" (progress); TUI `hejje baskets [id]`, `hejje splits`. Deviations/assumptions: legs are placed strictly one at a time and each waited on (explicit legging, simplest safe reading of "hedges first"); the basket-level risk is the combined margin check, each leg is still risk-checked on its own (so options spreads without per-leg stops wait for M5.4's options risk); **the account's `reentryCooldown` counts from the last fill of any kind on the instrument, so it blocked every split child after the first; split children now waive exactly that check through a new `RiskEngine.evaluate(intent, waived, reason)` / `ExecutionEngine.submit(command, waived, reason)`, recorded in the child's risk decision as `waived: child of split …` (the whole intent passed it; every other check still applies to each child)** — the cooldown itself is unchanged for all other orders; REST prices are strings (`"1495.00"`), as elsewhere in the API (`Price` has no creator for JSON floats; my first test bodies sent floats and got 400). Tests: `ExecutionPlannerTest` (PRD example, sells, from/to flat, no-op), `BasketIT` (ALL_OR_NOTHING + CLOSE_FILLED_LEGS with the fake broker rejecting leg 2: leg 1 FILLED → ROLLED_BACK and flat, leg 2 FAILED, leg 3 never placed; hedge leg placed first; BEST_EFFORT → PARTIAL with the risk refusal named; combined margin beyond funds → FAILED with no order; REST key required + idempotent; agent `submit_basket_intent` → BASKET_NEW approval → a human key approves → basket COMPLETED with reason AGENT_PROPOSAL), `SplitIT` (REST split 250 → children 100/100/50 with ≥ 600 ms between the three and the waiver recorded on children 2-3; deadline → EXPIRED after the first child; +1.33 % move vs 1 % tolerance → CANCELLED; whole intent over max quantity → 422 and nothing placed; smart −50 → +100 = BUY 150 over REST, no-op, both fields 400), web `baskets.test.ts`, TUI `TestRenderBasketShowsLegsInPlacementOrderAndSplitsProgress`; `docs/agent-tools.md` regenerated. Full suite 540 green; web lint/test (36)/build green; TUI vet/test/build green. |
| M5.4 | 2026-09-10 | (this commit) | Options analytics and multi-leg strategies: new `options` module. **Black-76** (`Black76`: price, delta, gamma, vega per vol point, theta per day on the futures price; implied volatility by bisection, null outside what the model can produce; West's double-precision normal CDF). **Chain** (`OptionChainService`, `GET /instruments/options/chain?underlying=&expiry=` and `/instruments/options/expiries`): per strike call/put quotes with IV and greeks, the forward from the nearest future expiring on or after the options (else the index spot as a proxy, noted), ATM strike, put/call ratios of OI and volume, max pain (the chain strike minimising the total expiry payout to holders); a request subscribes the chain instruments in FULL mode (`MarketService.subscribeFull`, new: OI only comes with full-mode ticks). **DSL**: `legs[]` (`action` buy/sell, `option` directional/opposite/ce/pe, `strike` atm / `{offset}` (out of the money for the type) / `{delta}`, `expiry` nearest/next/monthly, `lots`, `stop_pct`, `target_pct`, `hedge_first` on buy legs only; at most four) and `combined_exit {stop_rupees, target_rupees}`, only with `family: options` (which needs legs); the new `StrategyDefinition` components are left out of the JSON when empty, so existing definition hashes do not change (tested); `docs/strategy-schema.json` and the DSL doc updated. **Execution**: `OptionLegResolver` resolves the legs on the chain at execution (today's expiry skipped from the 13:00 cutoff), `OptionsExecutor.open` places them as an ALL_OR_NOTHING basket with CLOSE_FILLED_LEGS rollback (hedge legs first and filled before the next, M5.3) and records an `options_position` (V30); signals of options strategies route there from `POST /signals/{id}/execute` (response: the options position), approvals and AUTO (`SignalService.isOptions/executeOptions/executeOptionsAuto`); the runner drops the signal. `OptionsPositionMonitor` (every `hejje.options.monitor-interval`): PENDING → OPEN with the entry fills when the basket completes (FAILED otherwise), then the first of force exit, the underlying crossing the signal's stop, a leg's premium stop/target or the combined P&L stop/target closes the position — short legs bought back first, long legs sold once the shorts are flat (STRATEGY_EXIT, exposure-reducing, up to three attempts per leg) — with realized P&L and `OPTIONS_POSITION_OPENED/CLOSED` audits; `max_trades_per_day` counts options positions; `GET /options/positions[/{id}]`. **Options risk** (`OptionsRiskCheck`, a risk contributor): `optionsLots` (`max-lots` 10), `optionsPremium` (buy premium at risk ≤ `max-premium-rupees` 50,000), `optionsDefinedRisk` (a sell needs long options of the same underlying, expiry and type covering it plus existing shorts, else refused unless the strategy's deployment sets `allow_undefined_risk: true`), `optionsExpiryDay` (no new option positions on their expiry day from 13:00); the stop-based account checks (`mandatoryStop`, `minRewardRisk`, `maxStopDistance`) pass with a note for option legs, everything else still applies; basket margin is checked on all legs together (M5.3). **Lifecycle/backtests**: the historical store has no option candles, so the backtester refuses options strategies and they are PAPER-only — DRAFT → PAPER directly, never BACKTESTED/VALIDATED, LIVE after `hejje.options.min-paper-trades` (30) closed paper options positions (`strategy.OptionsPaperEvidence`, implemented by the options module). **Strategies**: `nifty_orb_call_buy` (the nifty_orb signal buying one ATM weekly call, 30 % / 60 % premium stop/target) and `nifty_bull_call_spread` (ATM call hedge first + call 100 points higher sold, combined ₹2,500 stop / ₹4,000 target; the bear put spread is the same legs with direction short), with docs. Web: `/options` (chain with ATM highlight, forward, PCR, max pain; options positions) and the Lab "Option legs…" builder; TUI `hejje chain <underlying> [expiry]`. Deviations/assumptions: the backtest branch of task 4 is not built (no option history) — options strategies are PAPER-only as the plan allows, and options strategies never reach AUTO (no backtest, hence no score; and live AUTO counts strategy positions as paper history); the forward for weekly options is the nearest future expiring on or after them (a small carry difference to the true forward); per-leg stops/targets are software exits managed by the monitor, so the leg orders carry none (and `riskPerTrade` sees no stop for option legs); defined risk is judged from the account's open option positions of the same underlying/expiry/type, which hedge-first basket legging makes true for spreads; `direction: both` is not supported for options (the runner treats it as long), so bull and bear spreads are separate definitions. Tests: `Black76Test` (values from an independent Python implementation, put-call parity, IV round trip and impossible prices, CDF accuracy), `OptionLegResolverTest` (ATM/offset/off-grid/delta picks, premium rounding), `OptionLegsTest` (bundled spread parsed, per-leg error paths, family/legs rules, JSON without legs unchanged), `StrategyLifecycleTest` (options transitions), `OptionsIT` (chain forward/ATM/IV/delta against the Python values, PCR and a hand-computed max pain over REST; legs for bull and bear directions, premium levels, expiry-day rollover; naked short refused without stop-control noise, covered short accepted, deployment allowance, lots/premium limits, expiry-day cutoff; spread: hedge-first basket → OPEN → stays open inside the combined stop → COMBINED_STOP closes the short leg first, realized = sum of legs, audit, paper evidence 1, `max_trades_per_day`; PAPER-only lifecycle and backtest refusal; an options signal executed over REST returns the options position and fills), `BundledStrategiesTest` and `ParityTest` updated for the two options strategies, web `options.test.ts`, TUI `TestRenderChainMarksAtmAndShowsModelValues`. Full suite 559 green (last run; `docs/agent-tools.md` regenerated for the new audit types). **Known intermittent test**: `OptionsIT.aSpreadOpensAsAHedgeFirstBasketAndClosesOnTheCombinedStopShortLegFirst` failed in two of the last three full-suite runs and never in isolation — once the opening basket was still executing after 15 s, once its short leg was still unfilled at the 30 s leg timeout (basket rolled back); root cause not found. The basket/split/AUTO workers were moved from virtual to platform threads (they block on JDBC inside `synchronized` code) and the test now fails with each leg order's state history and the fake broker's view, to diagnose the next occurrence. The same run exposed a race in `AutoExecutionIT` (the signal is EXECUTED just before the `AUTO_EXECUTED` audit row is written), fixed by waiting for the row. Web lint/test (39)/build green; TUI vet/test/build green. |
| M5.5 | 2026-09-10 | (this commit) | Notifications and external webhooks. **Notifications** (new `notify` module, PRD 58): the 16 PRD types (`NotificationType` with default severity) come from domain events — signal (plus `HIGH_SCORE_SETUP` at score ≥ `hejje.notify.high-score` 80), order REJECTED, STOP-role fill, position flat, kill switch (plus `DAILY_RISK_THRESHOLD` when it tripped on `DAILY_LOSS`), broker session leaving CONNECTED, egress MISMATCH, and the existing client pushes `drift`, `approval` (PENDING only) and the new `llm_budget` (`LlmService` now publishes it with the budget audit) — handed to one `notify-events` platform thread so the trading core never waits; readiness turning off, a HIGH-risk event within `event-lead` (60 min, once per event) and a deployed instrument's news label changing come from a one-minute watcher (transitions only). `NotificationService.notify`: dedupe by key within `dedupe-window`, inbox row (`notification`, V31), then per enabled `notification_rule` (type × channel × min severity; seeded: all in-app, email/Telegram for all but the frequent SIGNAL_GENERATED / POSITION_CLOSED / NEWS_CONTEXT_CHANGED): in-app at once (`/ws/events` `notification`), email/Telegram on a background outbox thread — SKIPPED with the reason when the channel is not configured, DIGESTED above the channel's `per-minute` and sent as one digest every `digest-interval` — every attempt in `notification_delivery` (QUEUED/SENT/FAILED with the error/SKIPPED/DIGESTED/DIGEST_SENT). Channels: `EmailSender` (spring-boot-starter-mail, `spring.mail.*`, off unless `hejje.notify.email.enabled` + from/to), `TelegramSender` (JDK HttpClient `sendMessage`; token only from `HEJJE_TELEGRAM_BOT_TOKEN`, never in status, errors or logs). REST: inbox + read (`market:read`), deliveries, rules GET/PUT (audited `NOTIFICATION_RULE_UPDATED`), channel status, `POST /notifications/test` (`admin`). **Webhooks** (new `webhook` module, PRD 59): admin CRUD (`webhook`, V31): server-generated secret shown once (create, rotate), stored with `TokenCipher`; target a strategy version or none = MANUAL_EXTERNAL; allowed instruments; HMAC (`X-Hejje-Timestamp` + `X-Hejje-Signature: sha256=hex(HMAC(secret, ts + "." + body))`, constant-time) or PASSPHRASE mode for TradingView (secret + timestamp in the body; replay key = body hash). Public `POST /api/v1/webhooks/{id}` (SecurityConfig permitAll for exactly one path segment): 401 signature/passphrase or timestamp outside ±`replay-window` (5 min), 409 replay (the ACCEPTED row claims the key under a unique partial index, so concurrent replays also lose), 403 disabled, 404 unknown, 400 not JSON, 422 validation/mapping (instrument allowed and resolvable, stop on the losing side, target on the winning side, entry defaults to the last price; strategy webhooks: an enabled unpaused deployment of the version in the server mode covering the instrument, direction matching the strategy, inside the trade window; option-leg strategies refused at creation). Accepted: `SignalService.createExternal` stores an ACTIVE signal of the deployment (evidence: webhook, note; audit SIGNAL_CREATED; `SignalGeneratedEvent`) → shown in Today; autonomy 4-5 decides through AUTO unchanged; below 4 the policy engine is asked (actor WEBHOOK) and a non-DENY gets an approval via `proposeHeldSignal` (requester type `WEBHOOK`); MANUAL_EXTERNAL → the new `ApprovalService.proposeExternalOrder` (manual proposal sizing and risk, intent reason WEBHOOK, requester type WEBHOOK). Every delivery logged in `webhook_delivery` (payload without the passphrase) and audited `WEBHOOK_RECEIVED`. Web: Settings gains the channels, rules matrix, test button, inbox and the webhooks panel (create, secret shown once with the URL, enable/disable, rotate, deliveries); a toast (and a browser notification when permitted) on each push. TUI: `hejje notify list|test`. Docs: `docs/notifications.md`, `docs/webhooks.md` (flow, responses, TradingView template), `docs/api.md`, `docs/config.md`; `docs/agent-tools.md` regenerated (audit enum). | Background web push through a service worker is not built (the browser notification fires only while the web app is open). DAILY_RISK_THRESHOLD is the kill-switch trip on the daily loss limit, not an earlier warning at a fraction of it (follow-up if wanted). The watcher conditions (readiness, events, news) keep their baseline in memory (a restart starts fresh) and are not covered by an IT because the test scheduler is a no-op (their logic is small and reuses tested services). The public webhook endpoint uses the replay store instead of an Idempotency-Key (senders like TradingView cannot set headers). Webhook admin endpoints are `admin`-scoped and not transactional paths. | WebhookSignaturesTest (4: valid, tampered/other secret/timestamp, timestamp formats, constant-time compare), ChannelSendersTest (6: Telegram against WireMock incl. a failure that never names the token, email with a stub sender incl. the digest, skip reasons, the rate-limit decision), NotificationIT (5: rate limit → digest → DIGEST_SENT and the budget back a minute later; rules/severity/dedupe and the audit; a failing channel logged; domain events → inbox with IN_APP SENT and EMAIL/TELEGRAM SKIPPED; REST inbox/read/rules/channels/test and scopes), WebhookIT (5: signed intent → ACTIVE signal of the deployment in Today + PENDING approval requested by WEBHOOK, replay 409, tampered 401, stale 401, delivery log, secret encrypted and never listed; refusals with reasons — wrong direction, instrument, stop side, trade window, disabled 403, unknown 404 — and rotation invalidating the old secret; passphrase mode with replay and no passphrase stored; MANUAL_EXTERNAL proposal with intent reason WEBHOOK; autonomy 4 executes through AUTO with no approval); LlmUnitTest/LlmResilienceTest adapted to the event publisher; web notifications.test (5); TUI notify_test (2). Full suite 579 green (559 + 20); web 44 green; TUI vet/test/build green. |
| M5.6 | 2026-09-10 | (this commit) | Second broker adapter and active/standby execution. **Dhan** (`broker.dhan`, `docs/broker-dhan.md`, from the DhanHQ v2 docs): `DhanAdapter` over the REST API with the JDK HTTP client (`@ConditionalOnProperty adapter=dhan`, client id required): consent login with the API key (`generate-consent` → Dhan login → callback `tokenId` → `consumeApp-consent`; the callback now also takes `tokenId`) or a pasted 24 h access token (a JWT, checked with `/profile`); token only in the `access-token` header, never logged (tested); place/modify (full order, unchanged fields from the current order)/cancel, order book, trades, positions, holdings, fund limit, per-order margin calculator, quotes (`/marketfeed/quote` by segment → security ids), daily and 1/5/15/25/60-minute candles (90-day windows), instruments from the public compact security master (`DhanInstrumentCsv`: NSE series EQ, BSE equities, NFO/BFO futures and options, indices under Hejje's Kite names, tick size from paise, broker token `<segment>:<securityId>` because ids are unique only per segment); errors by code (DH-901/807–810 AUTH and drop the session with `BrokerAuthRejected`, DH-904/429 RATE_LIMIT, DH-905/907/81x INPUT, DH-903/906 REJECTED, DH-908/909/5xx NETWORK, timeouts TIMEOUT); market data by REST quote polling (`DhanQuoteStream`). Wired as the third raw adapter (paper-wrapped in PAPER mode). **Per-broker rate limits**: `hejje.broker.rate-limits.<broker>` (zerodha and dhan from their published limits; the global `limits` for others). **Contract**: `BrokerAdapterContractTest` (identity/session, place → read back as sent (instrument, side, qty, type, limit, tag, non-terminal) → in the order book → cancel → CANCELLED, quotes, account read shapes, every scripted broker error → kind with the right retryable/outcome-unknown flags, unmapped instrument → INPUT with nothing sent, clearing the session) with four harnesses: fake, paper (stateful stand-in delegate), Zerodha and Dhan against WireMock; to pass it the fake and paper adapters now refuse an instrument without a broker mapping (INPUT) like a real broker. **Broker accounts** (V32 `broker_account`, partial unique index = at most one active): logins register their account, the first becomes active, `POST /broker/accounts/{id}/activate` (admin, audited) switches in one transaction; the `brokerAccount` readiness check (execution module, to keep module dependencies acyclic) blocks orders while the connected account is not the active one (skipped for the fake broker). **Active/standby** (PRD 43): the executor lease gains `epoch` (V32; incremented on every change of owner — the fencing token), configurable `hejje.execution.lease.{ttl, heartbeat, instance, failover-hold}`, `release`, `failover` (release + hold back) and `checkFence` (re-reads the row right before every broker place/modify/cancel: refuses unless owner, epoch and expiry match; an unreadable lease refuses — fail closed; new `ExecutionException.NotActiveExecutor`, order REJECTED `NOT_ACTIVE_EXECUTOR`, REST 503); a failed renewal drops the lease; standbys skip the order poller, scheduled reconciliation, the unknown-order resolver and kill-switch actions; bootstrap goes back to waiting when the lease is lost and re-runs reconciliation on re-acquisition. `GET /server/executor`, `POST /server/failover` (admin, `{"confirmation":"FAILOVER"}`, audit `EXECUTOR_FAILOVER`), `hejje executor` / `hejje failover`, Server page (role, failover button, broker accounts). **Broker dimension**: `broker.call` and `broker.ack` tagged `broker`, latency rows and reconciliation issues (V32 column) carry it; Server page columns. RUNBOOK §9 (two VMs, shared Postgres, secondary static IP registered with the broker, controlled and uncontrolled failover); `docs/execution.md`, `config.md`, `api.md`; `docs/agent-tools.md` regenerated (audit enum). | Split brain is proven at the lease/fence level with simulated instances (separate connection paths over the shared test database, one cut to simulate a partition) plus the real order path in the single test context, not with two running Spring contexts (the suite must share one context). Dhan: market data is REST polling, not the binary websocket feed; order updates come from the poller and reconciliation (postback and the order-update websocket are not consumed); the multi-order margin endpoint is not used (its docs contradict themselves), so basket margins have no hedge benefit; 3-minute candles do not exist at Dhan; the 1000 orders/hour limit is not modelled; nothing was run against a live Dhan account (WireMock from the documented shapes; the consume-consent method and the cancel body are ambiguous in the docs and handled both ways). Switching the active account to another broker needs a restart with that adapter (one adapter bean per process). The Kite session-expiry schedule (06:15 IST) is used for Dhan's 24 h tokens (at or before their expiry for logins after 06:15). | DhanAdapterWireMockTest (8: consent login and secrets never logged, pasted token + profile, place/modify/cancel request bodies, orders/trades/positions/holdings mapping, quotes and candles incl. IST windows and the unsupported interval, every error family incl. session drop, timeout/unmapped, security master), DhanInstrumentCsvTest (2), BrokerAdapterContractTest × 4 adapters (28), BrokerAccountIT (2: one active, idempotent registration, activation in one transaction, the DB refusing a second active, REST scopes/404/note), BrokerAccountReadinessTest (2), ExecutorFailoverIT (7: racing acquisitions, partitioned active fails closed and cannot submit after takeover, paused active resumes as standby, controlled failover and hold-back, 600-step random schedule with 3 instances — one submitter per epoch, epochs monotonic, several takeovers — the real engine path REJECTED with nothing at the broker then sent after hand-back, REST failover scopes/confirmation/409); RateLimiter tests adapted to per-broker limits; web executor.test (2); TUI executor_test. Full suite 628 green; web 46 green; TUI vet/test/build green. |

## Phase 5 exit

All milestones M5.1–M5.6 implemented and committed on `main` (2026-09-10). Server: full suite green (628 tests, `ApplicationModules.verify()` passes; locally unpacked Gradle 8.14.5, Testcontainers Postgres). Web: lint, test (46), build green. TUI: vet, test, build green. `npx playwright test` against the dev stack (fresh Postgres, the boot jar of the M5.6 build, fake broker, PAPER, fixture LLM): 4 passed (smoke, paper flow, Hejje AI chat, approvals inbox). On the same stack: `GET /server/executor` reported `ACTIVE` at epoch 1 (the dev profile requires the lease), the built `hejje` binary ran `hejje executor`, `hejje notify test` (IN_APP SENT; EMAIL and TELEGRAM SKIPPED with the missing setting) and `hejje failover` (declined at the prompt), and the broker timers in `/server/latency` carried `broker=fake`.

Phase 5 exit checklist status:
- [x] AUTO executes only policy-eligible strategies — `AutoExecutionIT` (9: an eligible signal executes as the strategy with `AUTO_EXECUTED`, rule `auto_strategy` and the deterministic context; HIGH event risk holds it as an approval; the kill switch stops AUTO before any order; autonomy 4 leaves a same-day re-entry to a human; a spent daily budget blocks; autonomy 4–5 need an AUTO/PAPER deployment and paper history before LIVE; a restart mid-trade never enters twice; an LLM outage changes nothing) and `PolicyEngineTest` (only qualified AUTO strategies are allowed and every rule ahead of `auto_strategy` still holds; agents never execute automatically). External webhook signals take the same path (`WebhookIT`: autonomy 4 executes through AUTO, below 4 an approval).
- [x] Drift pauses a degraded strategy — `DriftIT.failedPausesOnceWithTheStatistics` (FAILED → PAUSE once, with the statistics in the audit), `autonomyFivePausesItselfOnceDriftIsDegrading`, `moveToPaperPausesALiveDeploymentAndRedeploysItInPaper`; overrides suppress and a worse status acts again.
- [x] Split-brain test passes — `ExecutorFailoverIT` (7): racing acquisitions never both win; a partitioned active fails closed and cannot submit after the standby takes over; a paused active resumes as a standby; controlled failover hands over without waiting and holds the old active back; a 600-step random schedule of partitions, pauses and failovers over three instances has exactly one submitter per epoch; and the real order path refuses at the broker call (REJECTED `NOT_ACTIVE_EXECUTOR`, nothing at the broker) until the lease is handed back. The M5.6 contract suite is green for all four adapters.

Follow-ups carried forward: from Phases 2–4 — VM backfill, baseline backtests and regime labelling of real history, curated event dates, the Playwright stack in CI, a smoke test against a real LLM provider on the VM, the others listed at the Phase 4 exit. From Phase 5 — run the Dhan adapter against a live account on the VM (static IP registered; the consent-consume method and cancel response are ambiguous in Dhan's docs), then consider Dhan's websocket feed and postback; provision the standby VM with its secondary static IP and rehearse the RUNBOOK §9 failover on real hosts (split brain is proven here with simulated instances on one database, not two processes); background web push for notifications and an early daily-loss warning; the intermittent OptionsIT spread failure noted at M5.4 (not seen in the M5.5/M5.6 full runs).

## Phase 6

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| M6.1 | 2026-09-19 | (partial, this commit) | **Costs verified** against zerodha.com/charges and NSE circular FATAX/73524 (Finance Act 2026, STT from 1 April 2026): NSE transaction charges 0.00307 % equity (was 0.00297), 0.00183 % futures (0.00173), 0.03553 % options (0.03503); STT futures 0.05 % (0.02), options 0.15 % (0.1); `verify: false` in both copies of `costs.yaml`. `CostModel` now charges options the flat ₹20 per executed order (Zerodha) instead of min(0.03 %, ₹20), which had understated a four-leg options position's brokerage ~15×. One rate set: backtests apply today's rates to all history (conservative for F&O before April 2026; stated in docs/costs.md and bakeoff.md). **Not done (VM, needs a CONNECTED Kite session and an admin key):** backfill and the coverage table in docs/data.md, the continuous-futures start date, regime labelling, the six baseline backtests. | CostModelTest (options flat brokerage). |
| M6.2 | 2026-09-19 | (this commit) | Indicators: `session_open/high/low`, `pivot`, `cpr_top`, `cpr_bottom`, `cpr_width_pct`, `supertrend(n, k)` (`Supertrend`, Wilder ATR, starts in an uptrend, continuous), `prev_day_nr(n)`, `opening_return(d)` (`OpeningReturn`) in `IndicatorFactory`, `IndicatorCatalog`, `RuleWords`, docs/indicators.md and strategy-dsl.md; `SessionTracker` keeps session high/low and the ranges of the last 64 completed sessions; the validator requires an `opening_return` duration to be a whole number of bars. Golden fixture extended to 12 sessions (900 rows) with pandas references for the new calls (TA-Lib has none). Deviations: `prev_day_nr` uses session ranges tracked from the intraday bars (as `prev_day_high/low` do), not daily candles, and ties count as narrowest; `opening_return` uses the first bar closing after 09:15 + d when the exact bar is missing. | IndicatorGoldenTest (29 columns, every bar within 1e-6), SessionIndicatorsTest (+2 hand-computed: CPR/NR/opening return/session levels; Supertrend flips both ways with crosses), ConditionParserTest (+13), RuleWordsTest. |
| M6.3 | 2026-09-19 | (this commit) | Eight definitions (`cpr_breakout`, `open_low_long`, `open_high_short`, `nifty_nr7_orb`, `stock_in_play_orb`, `nifty_intraday_momentum_long/short`, `supertrend_vwap`) with the plan's pre-registered values, docs pages (rationale, rules in words, source, regime fit, weaknesses, deviations) and README rows. `target: none` is not blocked by the risk pipeline (`minRewardRisk` passes an intent with no target), so no `risk_multiple 3` substitute was needed. NIFTY 50 universes list today's 50 constituents explicitly (the DSL has no universe reference). Deviations stated in the docs: `stock_in_play_orb` filters on thresholds instead of ranking the top 20 and measures relative volume on the signal bar against 20 sessions (the paper: first 5-minute bar vs 14 days) and is long only; the intraday momentum holding period is 14:45–15:10 (MIS). | NewStrategiesSignalTest (7: every new strategy on hand-built sessions — signal bar, side, entry, exit reason where relevant — plus a negative case for the distinguishing filter), BundledStrategiesTest, ParityTest (all bundled, 17 files). ExperimentAgentIT used `supertrend` as its "unknown indicator": now `ichimoku_base`. |
| M6.4 | 2026-09-19 | (this commit) | `direction: neutral` (options only; legs must be `ce`/`pe`; stop `points`/`percent`/`atr_multiple` becomes a symmetric band). A neutral signal keeps the non-null `side` column as BUY with the stop at the band's lower edge (sizes the Today dry run only); `SignalService.openOptions` turns it into a band around the reference price; `OptionsPosition.direction` is null for neutral, new `underlyingStopHigh` (V33 `underlying_stop_high`, direction stored as `NEUTRAL`); the monitor closes with `UNDERLYING_BAND` when the underlying leaves the band either way; the resolver refuses directional/opposite legs without a side; the backtester refuses neutral. `nifty_920_iron_fly` definition and doc (leg stop closes the whole position; expiry-day `nearest` is the expiring contract). **Policy change (plan task 4):** `auto_strategy` required a known Hejje Score, which options strategies can never have, so a PAPER autonomy-4 iron fly would have been held for approval daily; `PolicyRequest.unscoredPaper` (forced false outside PAPER) lets AUTO_ELIGIBLE match an options strategy in PAPER without a score; live AUTO is unchanged. Web Options page shows NEUTRAL. **Not done:** deploying it in PAPER at autonomy 4 on the VM. | OptionLegsTest (neutral only with options, directional/opposite legs refused, one-sided stop refused, rule words), OptionsIT (+1: the fly resolves to four legs, opens hedges first, band stored 24860/25160, stays open inside, closes `UNDERLYING_BAND` at the upper edge, a second fly at the lower edge), PolicyEngineTest (+1: PAPER-only waiver, rows ahead still hold). |
| M6.5 | 2026-09-19 | (tooling, this commit) | `research/tools/bakeoff.py` (stdlib; `plan` / `run` / `holdout`, `--latest` pins the holdout boundary): resolves series (continuous futures, index proxy for price-only rules when futures cover < 2 years), the common range ending 6 months before the latest session, submits WALK_FORWARD 12/3 NEXT_OPEN runs with 5/10 bps, polls, recomputes the score for the 2× slippage expectancy, pulls trend × volatility regimes, applies the pass rule and writes the table between markers. `docs/strategies/bakeoff.md` holds the pre-registered protocol (stability counted over test windows with at least one trade). Checked against a stubbed API only. **Not done (VM):** the runs, findings, holdout and PAPER deployments. | — |

Server suite 653 green (one fixed), web lint/typecheck/46 tests green.

## Phase 7

| Milestone | Date | Commit / PR | Notes, deviations, follow-ups |
|---|---|---|---|
| M7.1 | 2026-09-19 | (this commit) | `ExecutionMode.SIM` with `simulated()` (PAPER, SIM) used where paper rules applied (deploy version rules, AUTO qualification and the options score waiver, off-session validation, drift move-to-paper); `ModeProfileGuard`: SIM only under `sim`/`dev`, never `prod`, only `hejje.broker.adapter=fake`; V34 seeds SIM `risk_limits` (copied from PAPER) and kill switch; `sim` profile in application.yml. `SimClock` (set / advance / pause) under `HejjeClock` in SIM, starting at `hejje.sim.start`. Direct time reads replaced: `StrategyRunner` now takes a `HejjeClock` (was a `ZoneId`), `SignalController`, `ServerController` (ping, health), notification digests (`Sender.sendDigest(held, at)`), the replay source's empty-day close; `LlmService` already used the clock. New module `sim`: `SimJobs` (RUN/SKIP with a reason for all 26 Hejje jobs plus Spring Modulith's two `Moments` jobs), `SimScheduler` (discovers `@Scheduled` methods, fixed-delay/rate/cron on simulation time, registry order, coalescing within a step, refuses unregistered jobs), `SimTime` (the only place time moves: `startAt`, `advance`); in SIM Spring's task scheduler drops every task and `@EnableScheduling` is off. `deploy/docker-compose.sim.yml` (`hejje-sim` + `postgres-sim`, 127.0.0.1:8091, history mounted read-only), RUNBOOK §10, docs/simulation.md, config.md; web and TUI show a SIM banner. Deviations: `Ids.newId()` and the fake broker's order-id seed keep the wall clock on purpose (id namespaces, never part of a result). The plan's "fixture day at MAX and 60× gives identical trades" and "force exit at simulated 15:10" need the replay and speeds of M7.2, so they are verified there; here the scheduler fires a 15:10 cron at simulated 15:10 and the same steps fire the same jobs, and the result hash is defined with the replay. | SimClockTest (2), SimSchedulerTest (4: order, coalescing, initial delay, cron at 15:10, placeholders, restart, unregistered job refused, duration parsing), SimJobsCoverageTest (every `@Scheduled` in money.hejje is registered), ModeProfileGuardTest (+1), SimModeIT (a real SIM context on its own Postgres: clock at the configured start, no job on wall time, SIM risk rows, ticks → M1 candle closed by the pipeline job after one simulated minute, repeatable job order). Server suite 662 green; web 46 green; TUI vet/test green. |
| M7.2 | 2026-09-19 | (this commit) | `SessionReplay` (market): recorded ticks when a day has them, else each stored M1 candle as four ticks (open :00, low/high at :20/:40 in the conventional order, close :59, volume split as cumulative day volume) into the live `MarketPipeline`. `SimSessionService` + `SimController` (`/api/v1/sim/sessions`, scope `sim:run`, V35 `sim_session`): one session at a time; create validates days and M1/tick coverage, truncates the simulated ledger, resets the paper broker to the session capital, the pipeline and the strategy runners, writes risk per trade / loss halt / max positions into the SIM limits and starts at 09:15 PAUSED; `play`/`pause`/`step`/`cancel` with speed 1/10/60/300/MAX; a step moves the clock to each tick (jobs fire), feeds pipeline and broker, and settles (every `Drainable` — signal engine, AUTO worker, paper update thread — drained and no module event of the session in flight, twice); in SIM the tick bus and signal dispatch are inline. Result per session: fills, friction (position fees), net P&L, SHA-256 over the fills; sessions left open by a restart are FAILED. Fills: in SIM the paper adapter wraps the fake broker (`BrokerSimulation`), MARKET orders fill on the next replayed tick with `hejje.paper.slippage-bps`, stops when crossed; `PaperBrokerAdapter.reset`. Look-ahead guard: in SIM `MarketService` hides candles closing after the clock and quotes stamped after it. Instrument ids: `POST /instruments/export` writes the master with ids; a SIM instance imports it at startup (fake mappings), the compose file mounts it read-only; the `sim` profile no longer syncs the fake master. Two market fixes found by the replay, both affect live: the first minute of a day measured its volume against yesterday's cumulative (always 0 — hurt `relative_volume` on the opening bar) and derived bars (M3/M5/M15/H1) closed a minute late, when the next period's first minute closed (a 5m strategy acted at 09:31 on the 09:25 bar, unlike the backtester); both fixed in `CandleBuilder`. `spring-modulith-events-core` moved from test to main scope. Not done (VM): the NIFTY 50 M1 backfill (docs/data.md updated). The M7.1 checks deferred here pass: identical result hash with part of the session at 60× and the rest at MAX; a strategy's force exit fills at simulated 15:10:00. | SimReplayIT (2: 375 steps; scripted MARKET entry waits and fills on the next tick at open × 1.0005; SL-M fills at the crossing tick with slippage; 15 of 375 stored candles and 3 M5 visible at 09:30; friction and net P&L; one session at a time; 60×-then-MAX run has the same hash; a SIM deployment's signal confirmed at 09:30, entry at 09:30:00 and force exit at 15:10:00), SimModeIT (+1: master import keeps ids, fake mapping, idempotent), SessionReplayTest, CandleBuilderTest (+1 new-day volume), AuthIT scope list (+`sim:run`). Server suite 667 green. |
| M7.3 | 2026-09-19 | (this commit) | New module `bots` (V36 `bot`, `bot_decision`; `sim_session.warnings`). Registration (`POST /bots`): EXTERNAL/LLM bots get a generated backing strategy `bot_<name>` (new `StrategyFamily.BOT`: direction both, the bot's universe/timeframe, force exit 15:10; DRAFT → PAPER directly, not backtestable, no legs); STRATEGY bots run an existing strategy. `StrategyRunner`: a BOT runner never evaluates entries and keeps the decision's target; position management now uses the position's own side (a bot trades both sides; for strategies it always equalled the runner's); `tightenStop` (tighten-only). `SignalEngine`: `position`, `exitPosition`, `tightenStop`. `BotHub`: decision points at every closed bar of the bot's timeframe (or every N minutes of M1 bars) with bars (grouped per close time), quotes, positions, working orders, regime and Pulse; sender thread outside SIM, lockstep in SIM (the hub is a `Drainable`, so the replay's settle delivers points and waits up to `hejje.sim.decision-timeout`, else SKIPPED); latency p50/p90, points/answered/skipped; STRATEGY bots' signals execute as decisions in SIM. `BotDecisions`: once per (bot, point, instrument) — repeats return the recorded outcome; entries need a stop on the losing side, become signals of the bot's deployment (`createExternal`, evidence: decision id and thesis) and execute at once in SIM/PAPER (`executeAuto`), go through AUTO in AUTO, become approvals in CONFIRM; kill switch and existing position refuse; EXIT/TAKE_PROFIT flatten; MOVE_STOP tightens only. REST `/api/v1/bots…`, WebSocket `/ws/bot?token=&bot=` (scope `bot:decide`), client preset `bot` (`market:read strategies:read bot:decide`). SIM sessions validate `bots` and warn for LLM bots on days before their knowledge cutoff. `research/bots/example_bot.py` (stdlib, minimal RFC 6455 client, opening-range rule), docs/bots.md. **Found by the end-to-end run and fixed:** auth judged token expiry on the simulation clock, so a replay jumping hours expired the 15-minute JWT mid-session (401); `AuthTime` makes auth use wall time in SIM (the application clock elsewhere). Deviations: trades are attributed through signal evidence and `bot_decision.signal_id/order_id` (the `trade` table is unchanged); a `bot:decide` key may answer for any bot (keys are not bound to one bot yet); points go only to connected bots (an unconnected bot has no SKIPPED rows); CONFIRM routing is covered at the routing level because a CONFIRM instance needs the prod profile. | BotProtocolIT (2, SIM lockstep: 09:20 entry without a stop refused; 09:25 ENTER_LONG executed and filled at 09:25:00 1500.75; 09:30 MOVE_STOP moved; 09:35 EXIT filled 1501.25; 09:40 unanswered → SKIPPED; 09:45 kill switch → refused; attribution through the signal; stats (≥ 60 points, 1 skipped, p50/p90); repeat answer idempotent; routes per mode. A STRATEGY bot's signal executes without a confirmation and is force-exited), BotWebSocketIT (1: a research key is refused with POLICY_VIOLATION; a bot-preset key gets `connected` and a decision point with every section; the reply is recorded with latency; REST refuses an unknown action as REFUSED; view shows LLM cutoff and stats), AuthTimeTest, AuthIT scope list (+`bot:decide`). End to end on a local SIM jar + Postgres: the reference bot over `/ws/bot` answered 75/75 points (p50 0 ms), entered on the breakout (EXECUTED), moved its stop to break-even (MOVED), session DONE with 2 fills, friction ₹220.37, net ₹3,331.29. Server suite 671 green. |
