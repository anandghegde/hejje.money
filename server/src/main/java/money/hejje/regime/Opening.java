package money.hejje.regime;

/** PRD section 13 opening behaviour; the gap labels refine to continuation/rejection after the opening range. */
public enum Opening { GAP_UP, GAP_DOWN, FLAT, GAP_CONTINUATION, GAP_REJECTION, UNKNOWN }
