/**
 * Shared kernel: value types, ids, clock, event base classes and configuration shared by every module.
 * Open so that all nested packages are visible to other modules.
 */
@org.springframework.modulith.ApplicationModule(type = org.springframework.modulith.ApplicationModule.Type.OPEN)
package money.hejje.common;
