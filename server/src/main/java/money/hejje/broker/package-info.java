/**
 * Broker module: the broker-neutral {@code BrokerAdapter} contract, its Zerodha implementation, the deterministic
 * fake used in dev/test, and the broker session lifecycle.
 */
@org.springframework.modulith.ApplicationModule
package money.hejje.broker;
